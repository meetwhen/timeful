---
id: TASK-0115
title: Remove unused frontend packages
status: Done
assignee:
  - opencode
created_date: '2026-08-30 14:22'
updated_date: '2026-08-30 14:26'
labels:
  - frontend
  - dependencies
  - cleanup
dependencies: []
modified_files:
  - frontend/package.json
  - frontend/src/views/Landing.test.ts
  - frontend/src/views/Landing.minimal.test.ts
priority: low
type: chore
ordinal: 122000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
A usage audit of frontend/package.json (searching src, e2e, configs, hidden dotfiles, and npm scripts) found six packages with zero references outside package.json itself: moment (migrated to temporal-polyfill), @rive-app/canvas, canvas-confetti, @types/canvas-confetti, copy-image-clipboard, and @vimeo/player. Additionally, production code no longer imports @vimeo/player; only stale vi.mock("@vimeo/player") blocks remain in the two Landing tests. Remove these packages and the stale test mocks so the dependency list matches actual usage. Explicitly retained despite no direct grep hit: oxlint-tsgolint (kept by user decision), jiti (ESLint needs it to load eslint.config.ts), @types/node (used via "types": ["node"] in all tsconfigs), prettier-plugin-tailwindcss (active in .prettierrc), postcss (Vite CSS pipeline via postcss.config.cjs), swagger2openapi and openapi-typescript (used by the gen:api script).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 frontend/package.json no longer lists moment, @rive-app/canvas, canvas-confetti, @types/canvas-confetti, copy-image-clipboard, or @vimeo/player
- [x] #2 frontend/package.json retains oxlint-tsgolint, jiti, @types/node, prettier-plugin-tailwindcss, postcss, swagger2openapi, and openapi-typescript
- [x] #3 Stale vi.mock("@vimeo/player") blocks are removed from src/views/Landing.test.ts and src/views/Landing.minimal.test.ts and the tests still pass
- [x] #4 npm install completes without the removed packages and the lockfile is updated
- [x] #5 npm run lint, npm run typecheck, npm run build, and npm run test:unit all pass
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All acceptance criteria are satisfied
- [x] #2 All required unit tests pass. Documentation-only changes are exempt unless the user requests unit tests
- [x] #3 All required e2e tests pass. Documentation-only changes are exempt unless the user requests e2e tests
- [x] #4 Changed Markdown files are formatted with npm run format:markdown
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Remove moment, @rive-app/canvas, canvas-confetti, @types/canvas-confetti, copy-image-clipboard, and @vimeo/player from frontend/package.json (audit already completed during task intake; keep oxlint-tsgolint per user decision and the other flagged-but-used packages).
2. Read the two Landing test files and delete only the stale vi.mock("@vimeo/player") blocks plus any now-unused mock payload imports/variables they exclusively own; keep surrounding tests intact.
3. Run npm install in frontend/ to sync package-lock.json and prune removed packages from node_modules.
4. Run the required checks: npm run lint, npm run typecheck, npm run build, npm run test:unit.
5. Verify acceptance criteria with evidence, record final summary, mark Done.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
package.json on disk had newer versions than the intake snapshot (e.g. vue-router ^5.3.0, @playwright/test ^1.62.1); re-read the file before the remaining edits, no conflicts.

Confirmed zero remaining references to all removed packages with rg across the frontend directory excluding node_modules/dist and the lockfile, then verified the lockfile itself no longer lists them after npm install.

oxlint-tsgolint kept per explicit user instruction even though .oxlintrc.json does not enable tsgolint.

DoD e2e item marked satisfied as vacuous: the repo defines required frontend checks as lint/typecheck/build/test:unit (no e2e), and this change altered no import surface; browser E2E was not run.

No Markdown files were changed by this task (DoD #4 exempt); backlog/backlog.md diff is generated by Backlog MCP bookkeeping.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Removed six unused frontend dependencies and stale test mocks found by a usage audit of frontend/package.json (search covered src, e2e, config files, hidden dotfiles such as .prettierrc/.oxlintrc.json, and npm scripts).

Changes:
- frontend/package.json: removed moment, @rive-app/canvas, canvas-confetti, copy-image-clipboard, @vimeo/player, and @types/canvas-confetti. Retained oxlint-tsgolint (explicit user decision) plus packages with no direct grep hit but confirmed usage: jiti (loads eslint.config.ts), @types/node (\"types\": [\"node\"] in tsconfigs), prettier-plugin-tailwindcss (.prettierrc), postcss (Vite CSS pipeline), swagger2openapi and openapi-typescript (gen:api script).
- frontend/package-lock.json: synced via npm install; no removed package remains in the lockfile.
- src/views/Landing.test.ts and src/views/Landing.minimal.test.ts: removed stale vi.mock(\"@vimeo/player\") blocks; production code no longer imports that module.

Verification: npm run lint, npm run typecheck, npm run build, and npm run test:unit all pass (137 test files, 952 tests). Browser E2E was not run: it is not part of the repo's required frontend checks and no import surface or runtime behavior changed; unit, typecheck, and production build cover the removal.
<!-- SECTION:FINAL_SUMMARY:END -->
