# Session Handoff

- Created session: `ses_fe9c1029fffeQY6zUdA3F0fq6N`

## Session Objective

Wire the inbound-mail (Stalwart) stack into the shared Caddy edge so the mail hostname is served by the existing edge, and commit the support-mail artifacts. Status: complete.

## Motivation And Impact

The Stalwart stack (from the prior session) had no reachable HTTP path from the Caddy edge: the `mail.caddy` site defaulted to `127.0.0.1:8080`, which is unreachable from inside the Caddy container. Without the edge-network wiring, the admin UI/gateway at `https://mail.timeful.fun` would fail once deployed. Deployment to the VM remains pending.

## Current State

- Commit `3b663395` ("feat: add inbound mail via Stalwart") created on `main`, not pushed (branch is 1 commit ahead of `origin/main`).
- `compose.development.yaml` remains staged (the local-Listmonk `host.docker.internal` tweak), intentionally excluded from the commit.
- Uncommitted/unstaged: `docs/environments.md`, `flake.nix`, `frontend/backlog/inbox.md`, `frontend/requirements/functional.md`; untracked handoff files. Not touched this session.

## Completed Work

- `infra/support-mail/compose.yaml`: project now joins the external `timeful-edge` network (`networks.default.name: timeful-edge, external: true`); validated with `docker compose --env-file infra/support-mail/.env.example -f infra/support-mail/compose.yaml config --quiet`.
- `caddy/sites/mail.caddy`: default upstream changed to `http://stalwart:8080`.
- `.env.edge.example`: added `CADDY_MAIL_DOMAIN=mail.timeful.fun` and `CADDY_MAIL_UPSTREAM=http://stalwart:8080`.
- `infra/support-mail/README.md`: Caddy env guidance updated from VM-loopback upstream to the `stalwart:8080` edge-network upstream.
- Committed the support-mail feature per the commit skill (files staged by the user's prior session; `compose.development.yaml` unstaged at the user's choice).

## Important Decisions

- Keep support-mail as a standalone Compose project (`infra/support-mail/compose.yaml`) for separation of concerns, instead of adding the service to `compose.edge.yaml`. Confirmed by the user.
- The standalone project joins the existing external `timeful-edge` network so Caddy resolves `stalwart:8080`; the admin UI still binds `127.0.0.1:8080` on the VM.

## Decision Rationale

- The edge project (Caddy) and the mail stack have separate lifecycles and env files; a shared external network gives cross-project service discovery without merging them.
- The default upstream in `mail.caddy` was updated to `stalwart:8080` so the site works even when `CADDY_MAIL_UPSTREAM` is unset.

## Constraints And Assumptions

- The `timeful-edge` Docker network must exist before `docker compose up -d` for this stack (`docker network create timeful-edge`), as noted in `DEPLOYMENT.md`.
- VM deployment, DNS, cert import, verification, and hardening remain as documented in the prior handoff (`backlog/handoffs/handoff-2026-08-18T18-42-34Z.md`) and the stack README.

## Validation

- `docker compose --env-file infra/support-mail/.env.example -f infra/support-mail/compose.yaml config --quiet` — passed.
- `docker compose -f infra/support-mail/compose.yaml config --quiet` without `.env` — failed as expected (missing `STALWART_PUBLIC_URL`).
- No live edge/Caddy validation run; Caddyfile remains validated only by inspection.

## Remaining Work

- Push `main` (`3b663395`) when ready.
- Commit the remaining staged/unstaged local-Listmonk work (`compose.development.yaml`, `docs/environments.md`, `flake.nix`, backlog/requirements edits) separately.
- VM deployment per `infra/support-mail/README.md`: copy stack, fill `.env`, `docker network create timeful-edge` if absent, run wizard (ACME disabled, Manual DNS, Console logs), create `support@timeful.fun` mailbox.
- Publish DNS records; reload Caddy with `CADDY_MAIL_DOMAIN`/`CADDY_MAIL_UPSTREAM` from `.env.edge`.
- Import Caddy cert via `scripts/sync-certs.sh`; verify MX, external SMTP, IMAP login, `https://mail.timeful.fun`.
- Hardening: remove `STALWART_RECOVERY_ADMIN`, disable unused listeners, cron `sync-certs.sh`.
- Later: verify relay DKIM/SPF alignment before any DMARC `p=reject`.

## Next Recommended Action

Push `main` and deploy the stack on the VM following `infra/support-mail/README.md`, starting with network/DNS prerequisites.

## Handoff Relationships

Continues `backlog/handoffs/handoff-2026-08-18T18-42-34Z.md` (original Stalwart stack creation and runbook); the edge-network wiring described in that handoff's Remaining Work item 2 is now implemented here.

## Suggested Skills

- `commit` — for the pending push/commit work.

## Relevant Files

- `infra/support-mail/compose.yaml` — Stalwart service; now on the `timeful-edge` network.
- `infra/support-mail/README.md` — deployment runbook with updated Caddy env guidance.
- `caddy/sites/mail.caddy` — mail hostname site; default upstream `http://stalwart:8080`.
- `.env.edge.example` — now includes `CADDY_MAIL_DOMAIN` / `CADDY_MAIL_UPSTREAM`.
- `infra/support-mail/scripts/sync-certs.sh` — cert copy + reload action.
