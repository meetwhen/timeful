# Session Handoff

- Created session: `ses_fe4dfdcebffeddmdudHE8W37NI`

## Session Objective

Continue the Stalwart inbound-mail deployment started in prior sessions: bring the fixed edge/mail files to the production VM `timeful-cloud-ru` (`192.144.13.176`), deploy `infra/support-mail` to `/srv/mail` headlessly, and wire `mail.timeful.fun` behind the existing Caddy edge. Status: **deployed and provisioned; blocked at the DNS gate**. The VM-side stack is live and verified locally; only DNS-published (user-owned) A/MX records stand between the current state and external certificates/mail flow.

## Motivation And Impact

The domain needs a real mailbox (`support@timeful.fun`) receiving inbound mail over IMAP on `mail.timeful.fun:993`. Without the A/MX records the stack stays invisible to the internet: no Caddy cert for the admin UI, no external SMTP delivery, no IMAP access. The previous two handoffs created stack artifacts but never deployed; this session completed the actual headless deployment on the VM.

## Current State

- **Deployed**: `/srv/mail` stack running on the VM (`stalwartlabs/stalwart:v0.16.18`), normal mode, ports 25/993 published, 8080 bound to `127.0.0.1` only. Bootstrap/recovery vars removed from `.env`.
- **Provisioned**: wizard completed headlessly via `x:Bootstrap/set`; `support@timeful.fun` (Account `c`) created; permanent admin `admin@timeful.fun` (Account `b`) exists with a known password; recovery admin deleted from `.env`.
- **Edge wired**: `~/timeful/.env.edge` gained `CADDY_MAIL_DOMAIN=mail.timeful.fun` and `CADDY_MAIL_UPSTREAM=http://stalwart:8080`; Caddy reloaded with `--env-file .env.edge`; production (`timeful.fun`) and staging (`staging.timeful.fun`) both return 200 after reload. Caddy's ACME for `mail.timeful.fun` fails only because the DNS A record does not exist; it retries every 60s.
- **Blocked**: `mail.timeful.fun` A and `timeful.fun` MX are still not published (checked 2026-08-19 18:05Z). The user asked the record list but no propagation was confirmed by the end of the session.
- Local repo `main` is 4 commits ahead of `origin/main` (mail work); nothing pushed.

## Completed Work

- Local commits (not pushed): `b6af0aae` (edge compose `CADDY_MAIL_*` passthrough), `a3d0049d` (`STALWART_RECOVERY_MODE` passthrough + `.env.example` doc), `47fb6cdc` (`STALWART_RECOVERY_ADMIN` made optional in compose so it can be dropped post-provisioning).
- VM sync via tar: `compose.edge.yaml`, `.env.edge.example`, `infra/support-mail/**` copied to `~/timeful`; pre-sync backups under `/tmp/mail-before-sync/`. VM repo itself left untouched (no pull; controlled copy only).
- `/srv/mail` populated from `infra/support-mail`; `.env` written with `.fun` values, `CADDY_DATA_DIR=/var/lib/docker/volumes/timeful-edge_caddy_data/_data` (Caddy runs containerized on this VM, so the volume path replaces the `.env.example` default `/var/lib/caddy`), recovery admin pinned then removed, `STALWART_URL=https://mail.timeful.fun`, `STALWART_USER=admin@timeful.fun`.
- Headless wizard: `x:Bootstrap/set` with `serverHostname=mail.timeful.fun`, `defaultDomain=timeful.fun`, `requestTlsCertificate=false`, `generateDkimKeys=false`, `tracer={"@type":"Stdout"}`, `directory={"@type":"Internal"}`, `dnsServer={"@type":"Manual"}`.
- Account provisioning via `stalwartlabs/cli` container (`--network host`, `STALWART_URL=http://127.0.0.1:8080`, recovery admin auth): created `support@timeful.fun` (`create account/user`); set admin password via `update Account b`.
- Credential storage on the VM: admin password in `/srv/mail/.env` (`STALWART_PASSWORD`); support mailbox password in `/srv/mail/.mailbox-credentials` (root-only, 0600). No secrets in the repo or handoff.
- Nightly cert sync cron installed: `30 4 * * * /srv/mail/scripts/sync-certs.sh >> /srv/mail/sync-certs.log 2>&1`.

## Important Decisions

- **Domain is `timeful.fun`** (user-confirmed earlier): deployed as `mail.timeful.fun` / `support@timeful.fun`; `.app` naming is stale throughout the original `infra/support-mail` text and earlier handoffs.
- **Headless provisioning**: no browser on the VM; wizard answers were driven through the JMAP `x:Bootstrap/set` endpoint via the `stalwartlabs/cli` Docker image rather than the interactive WebUI, exactly as the vendored Stalwart docs describe.
- **Controlled file copy, not git pull**: the VM repo is 6 behind `origin/main` and the local `.fun` migration is not on `origin`; pulling risked unrelated churn, so only the mail/edge files were synced.
- **Recovery path used as designed**: `STALWART_RECOVERY_ADMIN` pinned the bootstrap credential; after the wizard and account/CLI work, the recovery variable was removed from `.env` and compose made it optional; the running server is in normal mode.
- **Caddy owns ACME on 443**; Stalwart `requestTlsCertificate=false`; DKIM generation disabled (outbound stays with the Brevo relay). TLS certs for SMTP STARTTLS/IMAPS are copied from Caddy by `sync-certs.sh`.

## Decision Rationale

- The Stalwart docs (`tmp/stalwart-website/src/content/docs/docs/configuration/{bootstrap-mode,recovery-mode,declarative-deployments}.md`, `install/platform/_setup-wizard.md`, `ref/object/bootstrap.md`, `management/cli/*.md`) were read and confirmed the headless paths before executing; this prevents a browser-only flow from stalling the deploy.
- Recovery mode was necessary because after the wizard the admin UI moves to `https://<hostname>/admin`; over plain HTTP only the recovery listener (8080) remains usable, and in recovery mode only the recovery admin sign-in is honoured (normal-account logins 403 there, as observed).

## Constraints And Assumptions

Confirmed:
- VM docker only via `sudo -n`; `timeful-edge` network and `timeful-edge_caddy_data` volume exist; ports 25/993 were free; ufw inactive; iptables INPUT ACCEPT.
- Only the production edge (`compose.edge.yaml` -> `caddy/Caddyfile`) imports `sites/*.caddy`; the staging edge compose (`compose.edge.staging.yaml` -> `caddy/Caddyfile.staging`) imports only `sites/staging.caddy`, so it does not need the mail passthrough.
- `sync-certs.sh` expects `CADDY_DATA_DIR` to point at the Caddy volume mountpoint on this VM.
- Disk headroom ~3.7 GiB on `/`; RAM 3.8 GiB.

Assumptions (unverified):
- User will publish `mail.timeful.fun` A -> `192.144.13.176`, `timeful.fun` MX -> `10 mail.timeful.fun.` (and optionally CAA `0 issue "letsencrypt.org"`, PTR at the hoster).
- Provider permits inbound TCP 25 to the VM (no external probe made).
- Brevo relay SPF/DKIM alignment is fine; `p=none` DMARC is safe as-is.

## Findings And Risks

- In normal (non-recovery) mode the admin login works over `http://127.0.0.1:8080`, so post-DNS CLI work can use `STALWART_URL=https://mail.timeful.fun` via Caddy or the loopback listener.
- Stalwart logs `DnsResolver` DNSSEC warning (DANE disabled) on every start; benign for this deployment (no DANE policy published).
- SMTP smoke test delivered to the local `support@timeful.fun` mailbox (DSN success), with the message marked as possible spam because `sender@example.org` has no SPF/DKIM - expected behavior for an unauthenticated test sender.
- Running listeners include `https` (443), `submissions` (465), `pop3s` (995), `sieve` (4190) inside the container; only 25/993/8080 are published via compose ports, so the others are not internet-exposed but are still enabled (hardening step below).
- Local repo `main` is 4 commits ahead; the VM now has these changes only as working-tree copies, so a future `git pull` on the VM will not be a clean fast-forward (no stashes created, but files are overwritten copies). The user has two pre-existing autostash entries from before this session.

## Dependencies And Prerequisites

- **DNS (user, blocking)**: publish `mail.timeful.fun` A -> `192.144.13.176` and `timeful.fun` MX -> `10 mail.timeful.fun.`; optionally CAA/PTR. Records verified absent as of 2026-08-19 18:05Z.
- After DNS propagates: Caddy auto-issues the LE cert; `/srv/mail/scripts/sync-certs.sh` copies it into Stalwart and reloads TLS certs (expects the CLI creds already in `/srv/mail/.env`).
- To let the VM catch up permanently, decide whether to push the 4 local commits to `origin` and rebase/reset the VM repo instead of keeping copied files.

## Validation

- `ssh timeful-cloud-ru` works; `sudo -n true` OK.
- `sudo docker compose -f /srv/mail/compose.yaml config --quiet` -> COMPOSE OK (also with the optional-recovery-admin compose).
- Bootstrap log line observed: `Server started in bootstrap mode ... Port 8080 is open for initial setup.` version 0.16.18.
- Wizard + account writes confirmed via `stalwartlabs/cli`: `update Bootstrap --file` printed `Updated Bootstrap singleton` + admin credential; `query Account` lists `support@timeful.fun` and `admin@timeful.fun`.
- Recovery mode restart observed (`Server started in recovery mode`), then normal-mode restart with no recovery/bootstrap log lines and listeners `smtp` 25, `imaps` 993, `https` 443, `http` 8080 up; MTA queue started.
- SMTP smoke test through the VM loopback: `ehlo sender.example.org -> mail -> rcpt support@timeful.fun -> data` returned `250 message queued` (queueId 324812364763365377); logs show local delivery + `DSN success`.
- Edge reload: `sudo docker compose --env-file .env.edge -f compose.edge.yaml up -d` OK; `curl https://timeful.fun` and `https://staging.timeful.fun` (via `--resolve` to `127.0.0.1`) both 200.
- Caddy ACME failure for `mail.timeful.fun` shows `DNS problem: SERVFAIL looking up A` - consistent with the records being unpublished; Caddy retries (`retrying_in: 60`).
- Not run: external SMTP/IMAP probes (need DNS); `sync-certs.sh` (needs issued cert); IMAP TLS login.

## Remaining Work

- Publish DNS (user) as listed under Dependencies; then confirm `dig A mail.timeful.fun` / `dig MX timeful.fun`.
- After the cert issues: run `/srv/mail/scripts/sync-certs.sh` (cron at 04:30 also handles it nightly); verify the cert in Stalwart (`tls.no-certificates-available` currently logged - expected until sync).
- Verify `https://mail.timeful.fun/admin` (admin UI) once DNS + ACME succeed.
- Verify IMAP TLS login on `mail.timeful.fun:993` as `support@timeful.fun`, and an external SMTP delivery (e.g. from a webmail provider).
- Hardening: disable unused listeners in the WebUI/CLI (submissions 465, pop3s 995, sieve 4190, https 443 inside the container - not internet-exposed but enabled); verify seed cron; optionally tighten DMARC to `p=reject` only after relay DKIM/SPF alignment is confirmed.
- Decide push vs copy: push the 4 local commits (254-side) and pull on the VM, or keep the working-tree copies; watch the two pre-existing git stashes on the VM.
- Consider extracting the `.mailbox-credentials`/`.env` secrets into a secrets manager or keeping them VM-only; they are currently VM-local only.

## Next Recommended Action

Ask the user whether the DNS records are published; once `dig A mail.timeful.fun` returns `192.144.13.176`, wait for the Caddy cert (retry loop is 60s), run `/srv/mail/scripts/sync-certs.sh`, then verify admin UI + IMAP TLS + an external SMTP message, and close the task in Backlog.

## Handoff Relationships

Continues `backlog/handoffs/handoff-2026-08-19T17-40-24Z.md` (VM reconnaissance, domain correction, confirmed headless wizard path) and the two earlier 2026-08-18 handoffs (stack artifact creation + edge wiring, both previously undeployed). This session performed the actual deployment and provisioning.

## Suggested Skills

- `commit` - push/commit the 4 local commits or amend when syncing the VM repo.
- `handoff` - if another handoff is needed after DNS completion.
- No deploy-specific skill exists in `.agents/skills`.

## Relevant Files

- `infra/support-mail/compose.yaml` - Stalwart service, now with optional recovery vars; synced to `/srv/mail` and `~/timeful`.
- `infra/support-mail/.env.example` - template; `.fun` naming, `CADDY_DATA_DIR` default still says `/var/lib/caddy` (incorrect for this VM; the host `.env` overrides).
- `infra/support-mail/README.md` - full runbook (wizard, cert import, hardening, backup).
- `infra/support-mail/scripts/sync-certs.sh` - cert copy + TLS reload; not yet successfully run (needs cert).
- `compose.edge.yaml` - mail env passthrough fix; synced to VM (uncommitted-to-origin, committed locally).
- `caddy/sites/mail.caddy` - edge site `{$CADDY_MAIL_DOMAIN}` -> `http://stalwart:8080`.
- `tmp/stalwart-website/src/content/docs/docs/...` - vendored Stalwart docs (bootstrap-mode, recovery-mode, setup-wizard, bootstrap object, CLI) used to drive the headless path.
- VM paths (not in repo): `/srv/mail/.env` (contains admin password), `/srv/mail/.mailbox-credentials`, `/tmp/mail-before-sync/` (pre-sync backups), `~/timeful/.env.edge` (mail vars added).
- `backlog/tasks/task-0038 - Deploy-Stalwart-inbound-mail-stack-to-the-timeful-cloud-ru-VM.md` - task record with the plan and progress notes.
