# Session Handoff

- Created session: `ses_fdaf4c435ffe3DmoS7VgL204uh`

## Session Objective

Continue the canonical functional-requirements migration from `backlog/backlog.md`. The approved six-record migration is complete and committed.

## Current State

Canonical functional requirements now run through `FR-029`. The worktree has unrelated changes in `flake.nix` and untracked pre-existing `handoff/` files; do not include or modify them for future migration commits unless explicitly requested.

## Completed Work

- Created and completed `TASK-0042.03`, a subtask of `TASK-0042`; its scope, decisions, and validation evidence are in `backlog/tasks/task-0042.03 - Migrate-additional-canonical-functional-requirements.md`.
- Added `FR-024` through `FR-029`, updated the requirements index, and removed only their duplicate source statements from `backlog/backlog.md`.
- Committed the migration as `d0dbc638` (`docs(requirements): add canonical functional requirements`).

## Important Decisions

- User approved all recommended candidates except the dates-only availability-overlay requirement. That excluded statement remains in `backlog/backlog.md` and was not canonicalized.
- `FR-024` through `FR-028` are `accepted` because their source backlog statements were completed. `FR-029` is `proposed` because browser-derived display-timezone initialization remains open.
- Requirements distinguish event timezone from display timezone, and event time format from display time format, following `frontend/glossary.md`.

## Decision Rationale

The migration only records durable, atomic, verifiable product behavior. Styling requests, investigations, implementation tasks, and unresolved product or authorization decisions are excluded until they become clear requirements.

## Constraints And Assumptions

- Canonical requirement files use stable ID-only filenames and the YAML schema in `docs/requirements/README.md`.
- Use the next contiguous identifier, `FR-030`, for the next new record.
- Keep `docs/requirements/functional-requirements.md` intact as a temporary migration source.
- Remove a source statement from `backlog/backlog.md` only after its canonical record and README index entry exist.
- Do not run Prettier with `--write` on the full backlog: it reformats unrelated legacy content. Check changed requirement Markdown only.

## Findings And Risks

- The backlog combines product requirements with visual polish, bugs, open questions, and infrastructure work. Reconcile a candidate against existing `docs/requirements/functional/` records before creating a new record to avoid duplication or contradictions.
- Timezone and timed-slot candidates need the terminology and semantic distinctions in `frontend/glossary.md`; when definitions matter, follow its links to ADR-012 and existing canonical requirements.

## Validation

The completed migration passed:

```sh
cd frontend && npm exec prettier -- --check ../docs/requirements/README.md ../docs/requirements/functional/FR-024.md ../docs/requirements/functional/FR-025.md ../docs/requirements/functional/FR-026.md ../docs/requirements/functional/FR-027.md ../docs/requirements/functional/FR-028.md ../docs/requirements/functional/FR-029.md
git diff --check
```

Structural checks confirmed six `FR-024` through `FR-029` IDs/headings, six README rows, retention of the excluded availability-overlay statement, and retention of the temporary legacy source. Unit and E2E suites were not run because the committed change is Markdown-only.

## Remaining Work

Continue selecting clearly stated, durable requirements from `backlog/backlog.md`. The excluded dates-only availability-overlay statement is still available for a later migration after user approval; it was intentionally not included in the current set.

## Next Recommended Action

Read `docs/requirements/README.md`, all nearby canonical records, `frontend/glossary.md` for any timed-slot candidate, and the relevant backlog source statements. Propose a small, atomic candidate set to the user before creating the next `TASK-0042` subtask. After approval, create the subtask through Backlog MCP, record its plan before editing, add each `FR-030+` record, index it, remove only its migrated backlog source, and run the Markdown-scoped checks above.

## Handoff Relationships

Continues the canonical migration initiated by `TASK-0042`; see its completed subtasks `TASK-0042.01`, `TASK-0042.02`, and `TASK-0042.03` for prior migration decisions.

## Suggested Skills

- `handoff` for a later durable transfer.
- `commit` when the next approved migration is ready to commit.

## Relevant Files

- `docs/requirements/README.md`: canonical format rules and Functional Requirements index.
- `docs/requirements/functional/`: existing canonical records; review before assigning a new identifier or duplicating a behavior.
- `docs/requirements/functional/FR-024.md` through `docs/requirements/functional/FR-029.md`: most recent migration examples.
- `docs/requirements/functional-requirements.md`: temporary legacy source; preserve unchanged.
- `backlog/backlog.md`: migration candidate source; remove only statements that have migrated.
- `frontend/glossary.md`: required terminology for timed-event slot, timezone, and time-format requirements.
- `backlog/tasks/task-0042 - Migrate-canonical-functional-requirements.md`: parent migration context.
- `backlog/tasks/task-0042.03 - Migrate-additional-canonical-functional-requirements.md`: latest approved scope and execution evidence.
