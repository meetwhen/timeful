name: timeful-test

services:
  mongo-test:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo_test_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  postgres-test:
    image: postgres:18.6-bookworm@sha256:7d2695c3aa88e792e8b3b233e7e4adb296a20412c6c0ca361e3edaaacfada108
    restart: unless-stopped
    environment:
      POSTGRES_USER: timeful_postgres_admin
      POSTGRES_PASSWORD: timeful_postgres_admin_password
      POSTGRES_DB: postgres
      POSTGRES_MIGRATOR_USERNAME: timeful_migrator
      POSTGRES_MIGRATOR_PASSWORD: timeful_migrator_password
      POSTGRES_APPLICATION_USERNAME: timeful_app
      POSTGRES_APPLICATION_PASSWORD: timeful_app_password
      POSTGRES_BACKUP_USERNAME: timeful_backup
      POSTGRES_BACKUP_PASSWORD: timeful_backup_password
    volumes:
      - postgres_test_data:/var/lib/postgresql
      - ./postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready --username \"$$POSTGRES_USER\" --dbname \"$$POSTGRES_DB\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  postgres-test-bootstrap:
    image: postgres:18.6-bookworm@sha256:7d2695c3aa88e792e8b3b233e7e4adb296a20412c6c0ca361e3edaaacfada108
    restart: "no"
    environment:
      PGPASSWORD: timeful_postgres_admin_password
      POSTGRES_USER: timeful_postgres_admin
      POSTGRES_TEST_DATABASE: ${POSTGRES_TEST_DATABASE:?POSTGRES_TEST_DATABASE is required}
      POSTGRES_MIGRATOR_USERNAME: timeful_migrator
      POSTGRES_APPLICATION_USERNAME: timeful_app
      POSTGRES_BACKUP_USERNAME: timeful_backup
    depends_on:
      postgres-test:
        condition: service_healthy
    command:
      [
        "sh",
        "-ec",
        "printf 'CREATE DATABASE :\"database_name\";\\nGRANT CONNECT ON DATABASE :\"database_name\" TO :\"migrator_username\", :\"application_username\", :\"backup_username\";\\n' | psql --host postgres-test --username \"$$POSTGRES_USER\" --dbname postgres --set=ON_ERROR_STOP=1 --set=database_name=\"$$POSTGRES_TEST_DATABASE\" --set=migrator_username=\"$$POSTGRES_MIGRATOR_USERNAME\" --set=application_username=\"$$POSTGRES_APPLICATION_USERNAME\" --set=backup_username=\"$$POSTGRES_BACKUP_USERNAME\" && printf 'GRANT USAGE, CREATE ON SCHEMA public TO :\"migrator_username\";\\n' | psql --host postgres-test --username \"$$POSTGRES_USER\" --dbname \"$$POSTGRES_TEST_DATABASE\" --set=ON_ERROR_STOP=1 --set=migrator_username=\"$$POSTGRES_MIGRATOR_USERNAME\"",
      ]

  postgres-test-migrate:
    build:
      context: ./server
      target: migrator
    restart: "no"
    environment:
      POSTGRES_MIGRATOR_URI: postgres://timeful_migrator:timeful_migrator_password@postgres-test:5432/${POSTGRES_TEST_DATABASE:?POSTGRES_TEST_DATABASE is required}?sslmode=disable
      POSTGRES_APPLICATION_USERNAME: timeful_app
    depends_on:
      postgres-test-bootstrap:
        condition: service_completed_successfully
    command:
      [
        "sh",
        "-ec",
        "goose -dir /migrations postgres \"$$POSTGRES_MIGRATOR_URI\" up && printf '%s\\n' 'GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO :\"application_username\";' 'ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO :\"application_username\";' | psql \"$$POSTGRES_MIGRATOR_URI\" --set=ON_ERROR_STOP=1 --set=application_username=\"$$POSTGRES_APPLICATION_USERNAME\"",
      ]

  server-test:
    extends:
      file: compose.yaml
      service: server
    build:
      target: builder
    working_dir: /app
    environment:
      MONGODB_URI: mongodb://mongo-test:27017/?serverSelectionTimeoutMS=2000&connectTimeoutMS=2000
      MONGODB_DATABASE: ${MONGODB_DATABASE:?MONGODB_DATABASE is required}
      POSTGRES_APPLICATION_URI: postgres://timeful_app:timeful_app_password@postgres-test:5432/${POSTGRES_TEST_DATABASE:?POSTGRES_TEST_DATABASE is required}?sslmode=disable
      APP_ENV: ${APP_ENV:-test}
      APP_BASE_URL: ${APP_BASE_URL:?APP_BASE_URL is required}
      SESSION_SECRET: "01234567890123456789012345678901"
      GIN_MODE: debug
      CLIENT_SECRET: ""
      MICROSOFT_CLIENT_SECRET: ""
      DISCORD_BOT_TOKEN: ""
      SERVICE_ACCOUNT_KEY_PATH: ""
      SLACK_DEV_WEBHOOK_URL: ""
      SLACK_MONETIZATION_WEBHOOK_URL: ""
      SLACK_PROD_WEBHOOK_URL: ""
      MAILCHIMP_API_KEY: ""
      MAILJET_API_KEY: ""
      MAILJET_API_SECRET: ""
      GMAIL_APP_PASSWORD: ""
      LISTMONK_ENABLED: "false"
      LISTMONK_PASSWORD: ""
      STRIPE_API_KEY: ""
      STRIPE_WEBHOOK_SECRET: ""
    ports: !override
      - "127.0.0.1:3003:3003"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3003/api/health"]
    restart: "no"
    volumes: !override
      - ./server:/app
    depends_on: !override
      mongo-test:
        condition: service_healthy
      postgres-test-migrate:
        condition: service_completed_successfully
    command: ["go", "run", "."]

  server-route-test:
    build:
      context: ./server
      target: builder
    working_dir: /app
    environment:
      GOCACHE: /tmp/go-cache
      MONGODB_URI: mongodb://mongo-test:27017/?serverSelectionTimeoutMS=2000&connectTimeoutMS=2000
      MONGODB_DATABASE: ${MONGODB_DATABASE:?MONGODB_DATABASE is required}
      POSTGRES_APPLICATION_URI: postgres://timeful_app:timeful_app_password@postgres-test:5432/${POSTGRES_TEST_DATABASE:?POSTGRES_TEST_DATABASE is required}?sslmode=disable
      ROUTES_READ_FILTERS_TEST_MODE: "compose"
      SESSION_SECRET: "01234567890123456789012345678901"
    volumes:
      - ./server:/app
    command:
      [
        "go",
        "test",
        "./routes",
        "-run",
        "TestGetEventFiltersUnnamedGuestResponses|TestGetResponsesFiltersUnnamedGuestResponses|TestGetResponsesBlindAvailabilityUsesFilteredGuestResponseMap|TestGetEventKeepsSignedInRowsWithBlankStoredResponseName|TestGetEventKeepsLegacyOuterKeySignedInSignUpRows|TestGetResponsesPrefersNewestCanonicalGuestDuplicate|TestAnonymousTimedEventCompatibilityContract|TestAnonymousDatesOnlyEventCompatibilityContract|TestAnonymousEventEditCompatibilityContract|TestAnonymousGuestResponseOwnershipCompatibilityContract",
        "-count=1",
      ]
    depends_on:
      mongo-test:
        condition: service_healthy
      postgres-test-migrate:
        condition: service_completed_successfully

volumes:
  mongo_test_data:
  postgres_test_data:
