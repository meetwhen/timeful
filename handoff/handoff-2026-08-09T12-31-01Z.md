# Session Handoff

- Created session: `ses_0199251fdffer0eVBSb1qLeNC9`

## Completed Work

Executing the re-verified derived-enabled-slot refactor plan from `handoff/handoff-2026-08-09T11-24-49Z.md` (rounded: remove materialized `enabledSlots` everywhere; persist only `activeSlots`; derive the enabled domain from picked dates / weekly anchor week + timezone + slot-generation settings; server regenerates and validates).

**Phase 0 (done, uncommitted):** Amended `frontend/adr/012-frontend-timed-event-instant-slot-model.md` — enabled slots are now a derived, never-persisted domain (terminology, Decision, canonical-state rules incl. weekly anchor + empty-active fallback, slot-generation rule, DOW semantics, compatibility, consequences, acceptance scenarios). Touched up `frontend/glossary.md` (`enabled slots`, `range-generated`/`full-day`, `advanced slot editing` left as-is).

**Phase 1 (done, uncommitted, tested):**
- `server/models/event.go:73`: `EnabledSlots` field removed (stale DB field intentionally left).
- `server/routes/event_timed_slots.go` (rewritten, 209 lines): `timedEventPayloadFields` carries only `ActiveSlots`/`EventTimezone`/`SlotGeneration`/`TimedRecurrence`; `deriveEnabledSlots` regenerates the domain (specific-dates from `SelectedDays`; weekly anchored on earliest active instant, else `time.Now()` in event tz; ported the exact `daysUntil`/Sunday=0=7/`startOnMonday` filter logic from `eventEditorSchedule.ts:125-143` as `weeklyAnchorDays`, Go weekday = Temporal dayOfWeek % 7); wrapped windows roll into the next day; instant-stepped increments match Temporal elapsed-time `add`; `active ⊆ derived` keeps `errActiveSlotOutsideEnabled`. New errors: `errInvalidEventTimezone`, `errInvalidSlotGeneration`, `errInvalidSelectedDay`. `parseLocalTime` accepts `HH:MM:SS` and `HH:MM`.
- `server/routes/events.go`: `enabledSlots` dropped from create/edit payload structs, `normalizeTimedEventPayloadFields` calls, `event.EnabledSlots` writes, and both `@Param` annotations.
- `server/scripts/20260724_canonical_timed_events/main.go`: new `canonicalEventRow` script-local bson-tagged struct for decodes (`canonicalSlots`/`buildLegacySlots`/`validateTimedMetadata` take it), Q2-agnostic; still writes stale `enabledSlots` at apply time. Script now only uses `models` for `SlotGeneration`/`TimedRecurrence`.
- Regenerated `server/docs/{docs.go,swagger.json,swagger.yaml}` via `go run github.com/swaggo/swag/cmd/swag@v1.16.1 init --parseDependency` (diff is annotation-only, 18/18/12 deletion lines); `cd frontend && npm run gen:api` refreshed `types/api.ts` (-3 lines).
- Tests: rewrote `server/routes/event_timed_slots_test.go` (derivation unit tests: specific-dates NY fixture, weekly anchor-from-earliest-active, Saturday-anchor-following-week, startOnMonday Sunday filtering, NY spring gap, NY fall overlap, wrapped window, Lord Howe 30-min DST both directions, dedupe, error cases, active-outside-derived for both kinds) and `server/routes/events_timed_payloads_test.go` (create normalizes/persists active-only and GET omits `enabledSlots`; edit weekly round-trip deriving the 4-slot LA domain; new `TestCreateEventIgnoresUnknownEnabledSlotsAndDerivesTheDomain` for the old-frontend window; active-outside-derived rejects for specific + weekly; empty-active pin kept). Cross-side fixture correctness was proven empirically first with a scratch program (`/tmp/opencode/dstcheck/main.go`) that confirmed Go `time.Date` normalization matches Temporal `compatible` disambiguation (gap: shift forward, e.g. 2:00→3:00 EDT; fold: instant-stepping walks both offsets; Lord Howe 30-min shift).

## Important Decisions

- Verification depth chosen by user: full — unit + local Firefox e2e. Scope: all phases, one pass. Q2 (20260724 migration prod status) intentionally left open; implementation is Q2-agnostic.
- Go derivation semantics lock (R1/R4): instant-stepping with `time.Date`/`Add` (matches Temporal elapsed-time `add` for time-only durations); both sides expected to produce identical instant lists for the shared fixtures.
- Legacy decode (Phase 2, designed but NOT yet implemented): consumer reads show legacy events rely on `event.enabledSlots` === raw old carrier, with `activeSlots`/`times` fallbacks in consumers (`useCalendarGrid.ts:179-207`, `useScheduleOverlapController.ts:106-107/297`, `general_utils.ts:131`, `useEventScheduling.ts:322`, `specificTimesEditDraft.ts:151-153/226`). Plan: `fromRawEvent` legacy branch folds `raw.enabledSlots` → it is the only place allowed to touch the raw field; legacy domain = normalized old `enabledSlots` if non-empty else `times`; contract branch derives via new `getEventEnabledSlots` and drops actives outside the derived domain (Q1). Decide where the folded legacy domain lives (candidate: reuse internal `times` for the legacy domain and a normalized-actives-first `activeSlots`, since legacy `enabled == active` for active-only payloads and consumers already treat `times` as the last-resort carrier — unverified, needs a grep-of-readers pass before committing to this).
- Old `specificTimesActiveSlots` `!= null` (not `length`) check at `useCalendarGrid.ts:179` means legacy events decode with non-null empty `activeSlots` and consumers fall through to `times` — the refactor must preserve this exact fallthrough for legacy events or the grid silently loses the domain (R2).

## Findings And Risks

- Pre-existing repo state: three legacy Go scripts (`server/scripts/20230812_add_calendar_accounts`, `20230914_15_minute_increments`, `20240909_multiple_calendar_support_groups`) do NOT compile against current models (`go build ./...` fails on them). Unrelated to this work; build the touched packages only (`go build ./routes/... ./models/... ./scripts/20260724_canonical_timed_events/...`).
- `docker compose ... run --rm server-test` has a fixed `command` (`go test ./...`); extra args are treated as a container command (`-run` fails with exec error). Scoped runs need `... run --rm --no-deps server-test sh -c 'go test ./routes/ -run "..." -v'`.
- Test fixture gotcha: derived slots are UTC instants of event-timezone wall times (09:00 LA = 17:00Z); two unit-test fixtures initially asserted 09:00Z and failed — fixed, tests now green.
- Remaining frontend churn is the known-large R6/R8 surface (116+ `enabledSlots` refs across src tests, 11 e2e specs, 7 e2e helper/repro files).

## Validation

- `go test ./routes/ -run 'TestDeriveEnabledSlots|TestNormalizeTimedEventPayloadFields'` — PASS on host.
- `go test ./scripts/20260724_canonical_timed_events/` — PASS on host.
- `docker compose --env-file .env.development -f compose.yaml -f compose.test.yaml up -d mongo-test` then `... run --rm server-test` — full `timeful/server/routes` suite PASS (0.172s).
- Scoped verbose route run (`TestCreateEvent|TestEditEvent`): all 7 tests PASS, including `TestCreateEventIgnoresUnknownEnabledSlotsAndDerivesTheDomain` and the two reject-outside-derived tests.
- `go build`/`go vet` for the touched packages — PASS (only the three pre-existing legacy script failures remain).
- Scratch DST program verified gap/fold/Lord-Howe/wrapped expectations before encoding them as fixtures.
- No frontend gates run yet (no frontend src changes made yet); prior baseline gates were green per the earlier handoff.
- Mongo-test container is still running; test stack cleanup command is `docker compose --env-file .env.development -f compose.yaml -f compose.test.yaml down -v`.

## Remaining Work

Target: next session resumes the same refactor, one pass, full verification (unit + local Firefox e2e).

**Phase 2 — Frontend boundary** (next):
- `frontend/src/utils/timedEventSlots.ts`: new `getEventEnabledSlots(event)` (daysOnly → []; legacy → old-domain fold; contract → specific-date `generateTimedSlotsForDay` over picked days, or weekly anchor-week days with the same daysUntil port as Go). Drop `enabledSlots` inputs: `getSlotGenerationStartTime`/`getSlotGenerationEndTime` (fallback → `timeSeed`), `getTimedEventTimezone` (fallback → `timeSeed`), `buildTimedDateSeeds` (L395-399), `getTimedSlotCoverage`, `hasCanonicalTimedSlots`; contract guards `hasCompleteTimedEventContract`/`isTimedEventContractPayload` drop the `enabledSlots` term (handoff Correction: guards live in `timedEventSlots.ts:34-51`, used only at `transport.ts:314-315`).
- `frontend/src/types/index.ts`: remove `enabledSlots?` (L44).
- `frontend/src/types/transport.ts`: `fromRawEvent` (L312-401) legacy fold (see Important Decisions) + contract-branch derivation w/ Q1 actives-drop; `toRawEvent` drops the L428 computation and L461-463 fallback.
- Submit paths: `eventMutationBoundary.ts` (drop `enabledSlots` from `EventPatchInput` L18 and emit block L80-86, plus unused `times?` L29), `NewEvent.vue:894-895/939-940`, `NewGroup.vue:436-444` (preserve derives from `getEventEnabledSlots(props.event)`; drop `times` lines), `NewSignUp.vue:420-422` (drop `enabledSlots` + unused `times` args), `EventDescription.vue:143-144`.

**Phase 3 — Consumers** (`getEventEnabledSlots`-first with legacy fallbacks, preserving the `!= null` fallthrough order): `useCalendarGrid.ts` `specificTimesActiveSlots`/`specificTimesEnabledSlots`/`specificTimesCoverageSlots`/`canonicalTimedSlots`/`savedSpecificTimesWindow`; `eventDateRules.ts` `getEventDateSeeds` (contract/anchor branch BEFORE `dates`/`timeSeed` fallback — ordering matters for weekly); `general_utils.ts:132-134` `hasSpecificTimes`; `useEventScheduling.ts` `saveTempTimes` (base = `getEventEnabledSlots`, merge, active-only persist); `useSignUpForm.ts:177`, `useEventEditorState.ts:312`, `specificTimesEntryState.ts:12/41/74`, `dateFormatting.ts:73`. Draft state stays draft-local (`SpecificTimesEditDraft`, `eventEditorSchedule.ts`).

**Phase 4 — Tests + e2e + gate**: cross-side fixtures mirrored into `timedEventSlots.test.ts` (specific NY, weekly LA anchor, gap/fold/Lord-Howe, wrapped, weekly Saturday anchor + startOnMonday sets — mirror the Go lists exactly); new `getEventEnabledSlots` suite; `transportBoundaries.regressions.test.ts` updates (decode/round-trip/patch payload/contract guards; `useCalendarGrid.test.ts` must pass UNCHANGED as oracle); 18 src test files + `scheduleOverlapTestUtils.ts:45/61`; grep for runtime casts like `(event as { times?: … }).times` to catch stragglers (R6); e2e updates (11 specs + `helpers/timed-event-helpers.ts` `readCreatePayload`/`buildCreatePayload` + `helpers/firefox-timed-event-harness.ts` + 6 `repro/*.ts`, R8). Then gates: `cd frontend && npm run lint && typecheck && build && test:unit`; Go suite via compose.test.yaml; Firefox e2e per root `AGENTS.md` (docker mongo+server with compose + compose.development, `npm run dev -- --host 127.0.0.1 --port 4173`, `PLAYWRIGHT_USE_EXISTING_SERVER=1 npm run test:e2e -- --project=firefox-desktop`). Commit only when the user asks.

## Handoff Relationships

- Continues `handoff/handoff-2026-08-09T11-24-49Z.md` (the source plan; Phases 0-1 executed, Phases 2-4 open). Supersedes the prior plan's Phase 1 detail template: server derive is implemented with the exact fixtures now locked in both test files.
- Prior handoff `handoff/handoff-2026-08-07T15-13-23Z` (enabled-inactive hover grey fix) remains unrelated, approved, unimplemented.

## Suggested Skills

- `commit` — when the refactor lands and the user asks for a commit.

## Relevant Files

Server (all modified, uncommitted):
- `server/routes/event_timed_slots.go` — derive + validate (rewritten).
- `server/routes/event_timed_slots_test.go` — unit fixtures now the Go side of the cross-side parity suite.
- `server/routes/events_timed_payloads_test.go` — route-level create/edit/reject/empty-active tests.
- `server/routes/events.go` — payload structs + `@Param` (create ~L176-238, edit ~L398-460).
- `server/models/event.go` — `EnabledSlots` removed (L73).
- `server/scripts/20260724_canonical_timed_events/main.go`, `main_test.go` — `canonicalEventRow` + updated tests.
- `server/docs/docs.go`/`swagger.json`/`swagger.yaml` — regenerated.

Frontend (Phase 2-4 work, currently unchanged):
- `frontend/src/utils/timedEventSlots.ts` — derivation hub to rework (exact line numbers in Completed Work/Remaining Work).
- `frontend/src/types/transport.ts` — `fromRawEvent` L312-401, `toRawEvent` L403-498.
- `frontend/src/types/index.ts` — `Event.enabledSlots?` L44.
- `frontend/src/composables/schedule_overlap/useCalendarGrid.ts` — slot-domain computeds L162-315, `times` cast L183.
- `frontend/src/utils/eventDateRules.ts` — `getEventDateSeeds` L39-72 (ordering constraint).
- `frontend/src/composables/event/eventMutationBoundary.ts` — `EventPatchInput` L14-41, payload L58-109.
- `frontend/src/composables/schedule_overlap/useEventScheduling.ts` — `saveTempTimes` L304-395.
- `frontend/src/utils/general_utils.ts` L125-186, `frontend/src/composables/event/specificTimesEditDraft.ts`, `frontend/src/composables/event/specificTimesEntryState.ts`, `frontend/src/composables/event/useEventEditorState.ts`, `frontend/src/composables/schedule_overlap/useSignUpForm.ts`, `frontend/src/utils/dateFormatting.ts`.
- Submit paths: `frontend/src/components/NewEvent.vue`, `NewGroup.vue`, `NewSignUp.vue`, `frontend/src/components/event/EventDescription.vue`.
- Tests: `timedEventSlots.test.ts`, `transportBoundaries.regressions.test.ts`, `useCalendarGrid.test.ts` (parity oracle), 18 total src test files.

E2E (R8): six `frontend/e2e/timed-event-*.spec.ts` + `schedule-overlap-pager-button-sizes`, `event-page-days-only-layout`, `event-description-header-layout`, `event-page-no-responses-layout`, `sign-up-form-event` specs; `helpers/timed-event-helpers.ts`, `helpers/firefox-timed-event-harness.ts`; `repro/repro-*.ts` (6 files).