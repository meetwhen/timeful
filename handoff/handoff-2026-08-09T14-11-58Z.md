# Session Handoff

- Created session: `ses_0193cd21affeuAOyvxpsTwnX4n`

## Completed Work

Executing the final Phase 4 tail of the derived-enabled-slot refactor from `handoff/handoff-2026-08-09T13-41-12Z.md` (remove materialized `enabledSlots` from the e2e surface; persist only `activeSlots`). All src-side work and the Go server side were already done by prior sessions.

**E2e updates — COMPLETE (all 92 refs handled):**
- `frontend/e2e/helpers/timed-event-helpers.ts` — `readCreatePayload` returns `activeSlots` only; `buildSeedPayload` keeps the `activeSlots ?? enabledSlots ?? []` convenience fallback but no longer emits `enabledSlots`; `EventApiPayload.enabledSlots` removed; `fetchEventByShortId` stops reading it; `CanonicalTimedSeedInput.enabledSlots`/`SpecificDateSeedInput.enabledSlots` now optional.
- `frontend/e2e/helpers/firefox-timed-event-harness.ts` — `TimedEventCanonicalSummary.enabledSlots` removed; `fetchEventByShortId` drops the read; `createSeedEvent` drops the `enabledSlots` input field AND the `enabledSlots = times` default (only caller updated; lint required the removal).
- 11 specs updated: 5 chromium layout/sign-up specs drop the key from seeds; the 6 `timed-event-*` specs flip `savedEvent.enabledSlots` equality to `not.toHaveProperty("enabledSlots")` and keep `activeSlots` equality:
  - `timed-event-create-firefox.spec.ts` — new `expectedCreatedDomain(created)` helper (09:00-16:45, 15-min rows, both selected dates) replaces `createPayload.enabledSlots`-derived expectations (`slice(0, 8)` = the drag selection).
  - `timed-event-specific-times-edit-firefox.spec.ts` — the "broad timed edits rewrite contradictory canonical slots" test was REPLACED (per the prior decision) with "stale active slots outside the derived domain are rejected at the boundary": raw POST with `activeSlots` containing instants outside the derived 09:00-17:00 UTC domain, asserting 400 + `{"error":"active-slots-must-be-enabled"}`.
  - `timed-event-reprojection-firefox.spec.ts` — the "subset summary" test had a seed that was contract-inconsistent under the new server (enabledSlots spanning a day outside `selectedDays`): `selectedDays` widened to `["2026-05-28","2026-05-29"]`, domain-key assertions removed.
- 6 repros updated: `enabledSlots` dropped from POST bodies; `canonical.enabledSlots` divergence checks replaced with `!("enabledSlots" in canonical)` absence checks; **`repro-viewer-tz-column-duplication.ts` needed a window change** — the old seed's actives (Jun 14 17:00-23:45 UTC + Jun 15 00:00-16:45 UTC) were outside any 09:00-17:00-derived domain, so `slotGeneration` was widened to a full-day window (`00:00`→`23:45`, 15-min) to keep the actives within the derived domain.

## Important Decisions

- Stale-slot e2e scenario is now the boundary-rejection assertion (see above); the frontend no longer has a "rewrite contradictory slots on save" flow to cover.
- E2e payload semantics locked: seeds may still pass `enabledSlots` as an input convenience (mapped into `activeSlots` at `buildSeedPayload`), but the emitted API payloads and all assertions are active-only + `not.toHaveProperty("enabledSlots")`.

## Findings And Risks

- **Firefox e2e run: 5 failed / 1 passed / 18 did not run** (serial-mode cascades; the diagnostic re-run was aborted by the user, so only the weekly failure's actual diff was captured):
  1. `timed-event-weekly-firefox.spec.ts:19` (real failure, captured): after the name-only edit save, `savedEvent.activeSlots` = `2026-08-10T16:00Z/16:30Z, 2026-08-12T16:00Z/16:30Z` (THIS week's Mon/Wed, 16:00Z = 09:00 PDT — the correct LA 09:00-10:00 window but anchored to the now-week) instead of the seeded `2026-01-05/01-07T17:00Z/17:30Z` (the anchor week of the earliest active). The weekly edit PUT is re-deriving the domain anchored at `now` instead of the earliest-active anchor — the weekly preserve path in the frontend (`toEventPatchPayload`/`preserveExistingTimedSchedule`/`eventEditorSchedule` re-anchor on save; suspected, NOT yet traced).
  2. `timed-event-create-firefox.spec.ts:40` — first test of a serial file (cascades to the other 4 create tests). Assertions: `createPayload.activeSlots ?? []` toEqual `[]` + `not.toHaveProperty("enabledSlots")`. Error output not captured; possible the create POST still carries `enabledSlots` from some submit path (e.g., `NewSignUp.vue`/`NewGroup.vue` before the specific-times grid?) or `activeSlots` is non-empty at POST — needs the full failure output.
  3. `timed-event-reprojection-firefox.spec.ts:27` — first test (cascades). Unknown error.
  4. `timed-event-specific-times-edit-firefox.spec.ts:25` — first test (cascades). Unknown error.
  5. `timed-event-utc4-edit-firefox.spec.ts:13` — standalone; the previous (pre-refactor) variant asserted 48 grid cells / 4 white. Unknown error.
- Port 3002 note: the base-compose prod server `timeful-server-1` (5-day-old **pre-refactor** build, `docker compose -f compose.yaml up`) was `docker stop`ped to free 127.0.0.1:3002; the dev stack `timeful-development-server-1` (fresh refactor build) now serves 3002. Decide whether to restart the old prod container after the e2e verification.
- Environment currently running: dev-stack mongo (`timeful-development-mongo-1`) + server on 3002, vite dev server on 4173 (nohup PID 28951, log `/tmp/opencode/vite-dev.log`), test Mongo (`timeful-test-mongo-test-1`) still up.
- All of the above e2e edits were verified by grep: remaining `enabledSlots` refs in `frontend/e2e` are only absence-assertions and seed-input convenience carriers.

## Validation

- `cd frontend && npm run lint` — 0 errors (3 pre-existing `vue/require-default-prop` warnings in RespondentsList.vue).
- `cd frontend && npm run typecheck` — clean (app + node + e2e tsconfigs; caught the `SpecificDateSeedInput.enabledSlots` required-ness, fixed).
- `cd frontend && npm run build` — succeeded.
- `cd frontend && npm run test:unit` — 136 files / 860 tests PASS.
- Go routes suite: `docker compose --env-file .env.development -f compose.yaml -f compose.test.yaml run --rm --no-deps server-test sh -c 'go test ./routes/ -count=1'` — ok; scoped verbose run (`TestDeriveEnabledSlots|TestNormalizeTimedEventPayloadFields|TestCreateEvent|TestEditEvent`): 23 PASS / 0 FAIL (incl. `TestDeriveEnabledSlotsLordHoweThirtyMinuteDtstMatchesFrontendFixture`, `TestCreateEventIgnoresUnknownEnabledSlotsAndDerivesTheDomain`, the two reject-outside-derived tests).
- `go test ./...` (all packages) still fails on the pre-existing unrelated `services/listmonk` nil-pointer test — unchanged, not caused by this work; the compose `server-test` service command is itself a scoped `-run` filter set (compose.test.yaml L28-36), not the full suite.
- `PLAYWRIGHT_USE_EXISTING_SERVER=1 npx playwright test --project=firefox-desktop` — 1 passed / 5 failed / 18 did not run (see Findings).
- Chromium-desktop e2e (the 5 layout/sign-up specs) NOT yet run.

## Remaining Work

Next session: diagnose and fix the 5 Firefox failures, then finish the gates.

1. Re-run the failing Firefox specs individually with full reporter output to capture each assertion diff: `PLAYWRIGHT_USE_EXISTING_SERVER=1 npx playwright test --project=firefox-desktop --grep "round-trips weekly|lands directly|reprojects a canonical|mobile grid does not show|UTC\+4" --reporter=list --workers=1` (dev stack + vite dev server on 4173 are still up). Do NOT abort the run before it finishes — the previous diagnostic run was aborted before errors were captured.
2. Fix the weekly re-anchor (root cause almost certainly frontend): trace the weekly edit PUT payload assembly — `toEventPatchPayload` (`frontend/src/composables/event/eventMutationBoundary.ts`), `preserveExistingTimedSchedule` in the submit paths, and `eventEditorSchedule.ts`/`getTimedWeeklyAnchorInstant` fallback order (`timedEventSlots.ts:337+`). The anchor must come from the earliest active instant; `timeSeed` fallback must only apply when actives are empty.
3. Capture and fix the other 4 failures (create payload semantics, reprojection, tooltip, utc4). Watch the known `readCreatePayload` quirk: it returns `undefined` for an empty `activeSlots` array (pre-existing mapping; test 1 uses `?? []`).
4. Re-run the full Firefox suite, then the chromium gate: `npx playwright test --project=chromium-desktop` (Playwright owns its own dev server; backend already on 3002 via the dev stack).
5. Cleanup: `docker compose --env-file .env.development -f compose.yaml -f compose.test.yaml down -v` (mongo-test stack). Decide whether to `docker start timeful-server-1` (old prod build) or leave the dev stack serving 3002.
6. Commit only when the user asks (single commit; exclude `frontend/backlog/inbox.md`).

## Handoff Relationships

- Continues `handoff/handoff-2026-08-09T13-41-12Z.md` (its steps 4-7: this session completed the e2e step-4 edits; the Firefox/Chromium runs and mongo-test cleanup remain, plus the new failure fixes).
- Prior handoffs `handoff/handoff-2026-08-09T11-24-49Z.md` / `12-31-01Z.md` / `13-14-12Z.md` remain the governing plan; `frontend/adr/012-frontend-timed-event-instant-slot-model.md` (amended) governs semantics.

## Suggested Skills

- `commit` — when the refactor lands and the user asks for a commit.
- `session-handoff` — this file was produced with it.

## Relevant Files

E2e (all edited this session, uncommitted):
- `frontend/e2e/helpers/timed-event-helpers.ts` — seed/payload/fetch helpers (active-only).
- `frontend/e2e/helpers/firefox-timed-event-harness.ts` — summary/fetch/createSeedEvent.
- `frontend/e2e/timed-event-create-firefox.spec.ts` — `expectedCreatedDomain` helper + active-only assertions.
- `frontend/e2e/timed-event-weekly-firefox.spec.ts` — FAILING (weekly re-anchor).
- `frontend/e2e/timed-event-specific-times-edit-firefox.spec.ts` — new stale-slot boundary-rejection test.
- `frontend/e2e/timed-event-reprojection-firefox.spec.ts` — subset-summary seed reworked.
- `frontend/e2e/timed-event-utc4-edit-firefox.spec.ts`, `timed-event-timezone-menu-firefox.spec.ts` — seed key dropped.
- Chromium specs: `schedule-overlap-pager-button-sizes.spec.ts`, `event-page-days-only-layout.spec.ts`, `event-page-no-responses-layout.spec.ts`, `event-description-header-layout.spec.ts`, `sign-up-form-event.spec.ts`.
- `frontend/e2e/repro/*.ts` (6 files; `repro-viewer-tz-column-duplication.ts` window changed to 00:00-23:45).

Frontend src (from prior sessions, suspected site of the weekly fix):
- `frontend/src/composables/event/eventMutationBoundary.ts` — `toEventPatchPayload`.
- `frontend/src/utils/timedEventSlots.ts` — `getTimedWeeklyAnchorInstant`/`getTimedWeekDays`/`getEventEnabledSlots`.
- `frontend/src/composables/event/eventEditorSchedule.ts` — weekly anchor conventions.
- Submit paths: `frontend/src/components/event/EventDescription.vue`, `NewGroup.vue`, `NewSignUp.vue`, `NewEvent.vue`.

Server (from prior sessions, unchanged; uncommitted): `server/routes/event_timed_slots.go`, `server/routes/events.go`, `server/models/event.go`, `server/scripts/20260724_canonical_timed_events/main.go`(+test), `server/routes/event_timed_slots_test.go`, `server/routes/events_timed_payloads_test.go`, `server/docs/*`.