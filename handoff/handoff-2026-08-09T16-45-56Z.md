# Session Handoff

- Created session: `ses_018a9f8f0ffeNl2OIC4D2vu3R2`

## Completed Work

Executed the approved plan from `handoff/handoff-2026-08-09T14-37-26Z.md` up to the weekly-fix and spec-update stage.

**Env prep.**
- Killed the three stale vite processes (PIDs 2909434/4173, 1785118/4174, 28963/4175) and started one clean `npm run dev -- --host 127.0.0.1 --port 4173` from `frontend/` (new PID 393531, log `/tmp/opencode/vite-dev.log`). Verified the `/api` proxy reaches `http://127.0.0.1:3002` (`VITE_API_PROXY_TARGET` from `.env.development:6`).

**Weekly re-anchor fix (done, regression-first).**
- Added unit regressions, confirmed RED against the bug then GREEN after the fix:
  - `eventEditorSchedule.test.ts`: weekly with fake now `2026-03-16` + `weeklyAnchorInstant` 2026-01-05 LA → dates/actives in the Jan anchor week, enabled == active (4 slots on Jan 5/7 17:00Z).
  - `specificTimesEditDraft.test.ts`: weekly event + anchored schedule + `specificTimesEnabled=false` → draft `activeSlots` stay the event's Jan actives (bug produced `2026-08-10/12` now-week slots).
- `eventEditorSchedule.ts`: added `weeklyAnchorInstant?: Temporal.ZonedDateTime` to `EventEditorScheduleInput`; weekly branch now anchors `const now = input.weeklyAnchorInstant ?? Temporal.Now.zonedDateTimeISO(...)`.
- `NewEvent.vue` submit: precomputes `timezoneValue`, passes `weeklyAnchorInstant: props.edit && props.event ? getTimedWeeklyAnchorInstant(props.event.activeSlots, timezoneValue) : undefined`; new import from `@/utils/timedEventSlots`. `NewGroup.vue`/`NewSignUp.vue` untouched.

**E2E verification.**
- `timed-event-weekly-firefox.spec.ts` test 1 FIRST HALF now passes (Jan actives preserved after name-only edit).
- Second half + `timed-event-utc4-edit-firefox.spec.ts` diagnosed as a stale-spec conflict (see Decisions) and updated:
  - weekly: `headerColumns` `["mon","tue","wed","thu"]`, `visibleDateStrings` `[]` — weekly columns never render a date line (`ScheduleOverlapTimeGrid.vue:68` renders it only for `isSpecificDates || isGroup`).
  - utc4: `totalCells` 72 (was 48; full-day Dubai domain projects to UTC Jun 23/24/25), `whiteCount` 4 unchanged.
- Run `timed-event-weekly-firefox.spec.ts + timed-event-utc4-edit-firefox.spec.ts` → 2 passed; the group test (weekly spec test 2) FAILED — first time it has run this session (serial mode skipped it before).

**Group roundtrip failure — empirical boundary check done.**
- Weekly spec test 2 fails at L128: `expect(savedEvent.type).toBe("group")` received `undefined`. `fetchEventByShortId` maps `type` only when in `{specific_dates, weekly, group}` (`timed-event-helpers.ts:335-338`), so the GET body's `type` was none of these (or absent).
- API-layer repro did NOT reproduce: seeded a group event via `POST /api/events` and `PUT` with a NewGroup-shaped payload (via `toEventPatchPayload` shape) → `GET` returns `type: "group"` (PUT 200). Root cause therefore NOT in server persistence; likely frontend-specific (picked-days/weekday normalization changing the PUT `timedRecurrence.kind`, or the group editor navigating before the save). Left a probe event `shortId 5Efb9` in dev Mongo as test data.

## Important Decisions

- **Update the two stale e2e assertions to FR-026, don't change the grid** (user-approved). Commit `659bc779` (Aug 2, "feat(frontend): project timed slots by display date") amended `frontend/requirements/functional.md` FR-026 to mandate display-timezone projected columns; the June-era e2e specs still encoded pre-amendment membership-day columns (`1/5, 1/7`; 48 cells), which are unreachable under the current design. The refactor's `useCalendarGrid.test.ts` (e.g. L1445: LA→Belgrade 3 columns) encodes the FR-026 semantics.
- Keep the event-aware weekly anchor (Option B from the governing handoff) — confirmed green end-to-end for the create/edit weekly flow.
- Standing scope: stop after gates; no `down -v`, no prod-container change, no commit; server changes only if the remaining failures truly need them.

## Findings And Risks

- R1 (confirmed): FR-026 says grid columns = slot's projected date in `Shown in`; the editing grid deliberately renders display-spill columns (full-day LA domain → 4 UTC columns Jan 5-8; Dubai → 3 columns Jun 23-25). utc4's original 48-cell expectation predates this and would have required reverting the design.
- R2 (open): group roundtrip `type` loss is NOT reproducible via raw API PUT/GET; not yet captured from the browser. Watch: `NewGroup.vue` `preserveExistingTimedSchedule` path (L422-451), `toEventPatchPayload` (`eventMutationBoundary.ts:56-99`), and whether the group PUT actually fires before `fetchEventByShortId` (helper `saveEditorAndWaitForPut` L530+).
- R3 (standing from prior handoff): create/reprojection/specific-times-edit specs remain unrun this session. Static review suggests they are spill-free (UTC-in-UTC flows; reprojection asserts `jun 2`/`jun 3` columns) but cell-state/payload assertions are still unverified.

## Validation

- Unit regressions: `npx vitest run src/composables/event/eventEditorSchedule.test.ts src/composables/event/specificTimesEditDraft.test.ts` → red (2 failed: Aug 10/12 received) before fix, green (24 passed) after.
- `PLAYWRIGHT_USE_EXISTING_SERVER=1 npx playwright test timed-event-weekly-firefox.spec.ts --project=firefox-desktop --reporter=list --workers=1` → test 1 passes through L85 second half after spec update; test 2 (group) fails at L128 `type` undefined.
- `timed-event-utc4-edit-firefox.spec.ts` alone → observed `White cells: 4, Total cells: 72`; updated expectation to 72 passes.
- API probe: `POST` group seed → `PUT` (200) → `GET` → `type: "group"`; no server change needed for the group failure so far.
- Scratch repro file `frontend/src/composables/event/scratch-repro.test.ts` created and deleted; its console output confirmed `schedule.dates` Jan 5/7 anchored and `enabledDays` Jan 5-8 (UTC spill) — the empirical basis for the FR-026 spec updates.

## Remaining Work

1. **Group roundtrip failure** (`timed-event-weekly-firefox.spec.ts` test 2): re-run the single spec, capture the PUT request body and GET response body (e.g. `playwright` network or a temp `console.log` in `fetchEventByShortId`), compare against `NewGroup.vue`'s `toEventPatchPayload` output; fix per captured diff (server allowed only if truly needed).
2. **Remaining three reds (from prior handoff, not yet run this session)**: run individually with full output, no abort: `timed-event-create-firefox.spec.ts`, `timed-event-reprojection-firefox.spec.ts`, `timed-event-specific-times-edit-firefox.spec.ts`; fix per captured diffs.
3. **Gates**: full Firefox suite, then `npx playwright test --project=chromium-desktop`, then `cd frontend && npm run lint && npm run typecheck && npm run build && npm run test:unit`; Go routes suite via `compose.test.yaml` only if server touched.
4. **Stop** (user decision): report final state. No cleanup (mongo-test stack stays, `timeful-server-1` as-is), no commit, no further handoff unless asked. Dev Mongo contains probe event `shortId 5Efb9` (group probe) from this session.

## Handoff Relationships

- Continues `handoff/handoff-2026-08-09T14-37-26Z.md` (its item 4's weekly fix is done and verified; failures 2-5 remain after the group test surfaced as an additional failure).
- Governing sources: `frontend/adr/012-frontend-timed-event-instant-slot-model.md`, `frontend/requirements/functional.md` FR-026/FR-021 as amended by commit `659bc779`.

## Suggested Skills

- `commit` — only if the user later asks (out of scope this round).
- `session-handoff` — this file was produced with it.

## Relevant Files

Frontend (changed this session, all uncommitted with the broader refactor):
- `frontend/src/composables/event/eventEditorSchedule.ts` — `weeklyAnchorInstant` input L12-23, anchor L129-131.
- `frontend/src/components/NewEvent.vue` — submit L859-875 (timezoneValue + weeklyAnchorInstant), import L579.
- `frontend/src/composables/event/eventEditorSchedule.test.ts` — new anchored-week anchor test (file tail).
- `frontend/src/composables/event/specificTimesEditDraft.test.ts` — new weekly preserve-without-specific-times test (file tail).
- `frontend/e2e/timed-event-weekly-firefox.spec.ts` — second-half grid assertions L81-88 (FR-026 comment); test 2 group failure at L128.
- `frontend/e2e/timed-event-utc4-edit-firefox.spec.ts` — L41-50: 72 cells expectation + comment.
- `frontend/src/components/schedule_overlap/ScheduleOverlapTimeGrid.vue` — L68 date-line render condition (weekly shows day names only).

Frontend (inspected, unchanged):
- `frontend/src/components/NewGroup.vue` — `preserveExistingTimedSchedule` L422-451, `toEventPatchPayload` call L428; group failure suspect path.
- `frontend/src/composables/event/eventMutationBoundary.ts` — `toEventPatchPayload` L56-99 (type included).
- `frontend/e2e/helpers/timed-event-helpers.ts` — `fetchEventByShortId` L322-359 (type whitelist L335-338), `saveEditorAndWaitForPut` L530+, `collectGridState` L626-674.
- `frontend/src/composables/schedule_overlap/useCalendarGrid.ts` — display-projection columns: `getSpecificTimesEditDays` L609-637, `allDays` L604-769, display-date cell keying L1021-1025.
- `frontend/src/composables/schedule_overlap/useCalendarGrid.test.ts` — FR-026 column tests L689-738, L1445-1552.
- `frontend/playwright.config.ts` — firefox-desktop fixed `timezoneId: "UTC"` L46-54 (basis for all spill math).
- Unrun specs: `frontend/e2e/timed-event-create-firefox.spec.ts`, `frontend/e2e/timed-event-reprojection-firefox.spec.ts`, `frontend/e2e/timed-event-specific-times-edit-firefox.spec.ts`.

Environment: clean vite dev server `127.0.0.1:4173` (PID 393531, log `/tmp/opencode/vite-dev.log`); `timeful-development-server-1` on 3002, dev mongo + `timeful-test-mongo-test-1` up; `timeful-server-1` stopped (leave as-is).