# Session Handoff

- Created session: `ses_01801a458ffeIZuH5Tcnr19Il2`

## Completed Work

Executed the first three steps of the approved plan from `handoff/handoff-2026-08-09T18-39-33Z.md` (docs + frontend derivation/draft/band), plus all associated unit-test rewrites. Interrupted mid-way through the schedule-overlap test updates: **the full frontend unit suite still has 7 failures** (old-contract expectations, see Remaining Work item 1).

**1. Docs amended (deliverable done).**
- `frontend/adr/012-frontend-timed-event-instant-slot-model.md`: terminology (enabled = always full civil day, window-insensitive; slot-generation = initial-active-range + metadata), Decision, timezone rules (re-anchor exception), slot-generation semantics, advanced-slot-editing (no range-generated vs full-day split; disable restores `active = enabled` = full day; wipe rule), picked-date semantics (added dates enabled-only), rendering (read-only band from actives, full axis only in editor / `Show all hours`), consequences, and Required Acceptance Scenarios. The Lord Howe window-start-in-fold edge note is retired (full-day domain never starts inside a fold).
- `frontend/glossary.md`: enabled-slots, slot-generation-settings, advanced-slot-editing entries updated; `range-generated versus full-day` entry replaced by `range-created event` + `saved active-range band`; new `wipe rule` and `re-anchor rule` entries.
- `frontend/requirements/functional.md`: FR-021 (full-day enabled always; wipe on save) and FR-026 (read-only collapsible band wording; range-event wording).

**2. Frontend source changes (uncommitted; docs files above are tracked and edited).**
- `frontend/src/utils/timedEventSlots.ts`: `getEventEnabledSlots` now derives the full civil day (00:00→next 00:00 exclusive, event timezone) per membership day with a window-insensitive `fullDaySlotGeneration`; new export `getEventWindowRangeSlots` (the persisted window's per-day range, used for range-vs-specific discrimination); `getTimedSlotCoverage` now computes from **actives** (fallback enabled) so `processEvent` start/end metadata for range events stays actives-shaped instead of widening to 00:00–24:00. `mergeActiveSlotsByMembershipDay` kept as-is (now has no production caller; only its unit test).
- `frontend/src/utils/general_utils.ts` (critical companion fix): `processEvent`'s `hasSpecificTimes` heuristic now compares actives against `getEventWindowRangeSlots` (legacy fallback to `getEventEnabledSlots`). With full-day enabled, `timedSlotsEqual(enabled, actives)` is always false, which would have flipped every timed event to `hasSpecificTimes = true` (range events would mis-render). The window-range comparison preserves the old semantics.
- `frontend/src/composables/event/specificTimesEditDraft.ts`: (a) added dates are enabled-only — the `mergeActiveSlotsByMembershipDay` call is replaced by `preserveActiveSlotsForPriorMembershipDays` (normalize, then filter to prior picked days by plain local-date mapping in the event timezone — safe because normalized actives are all in-domain); (b) edit draft returns `schedule.slotGeneration` (stored window survives; `applySpecificTimesEditDraft` derives duration from it); (c) disable branch restores full-day actives when `event.hasSpecificTimes === true`, otherwise regenerates the schedule window range (range events unchanged). Create draft untouched (still 00:00→00:00 + empty actives, per approved create contract).
- `frontend/src/composables/schedule_overlap/useCalendarGrid.ts`: `savedSpecificTimesWindow` band derives from `specificTimesViewSlots` (actives-first) with `specificTimesCoverageSlots` fallback; the read-only `splitTimes` axis and `timedGridSlotProjection` (rows) use the same actives-band source (fallback `canonicalTimedSlots`) instead of the full-day enabled domain. The editor state (`SET_SPECIFIC_TIMES`) still renders the full-day domain.

**3. Unit-test rewrites (all three targeted files green).**
- `frontend/src/utils/timedEventSlots.test.ts`: `getEventEnabledSlots` describe block rewritten to full-day contract (count + first/mid/last instant assertions, e.g. NY specific-date 96 @15min 05:00Z→04:45Z next day; weekly 48/day @30min; NY spring gap 92; NY fall overlap 100 with both fold occurrences; Lord Howe spring 94 / fall 98; UTC wrapped-window day now full 48; plus a window-insensitivity assertion). New `getEventWindowRangeSlots` describe block.
- `frontend/src/composables/event/specificTimesEditDraft.test.ts`: added-dates test flipped to enabled-only (actives stay 2, none on the added day); disable test now expects full civil day (2×96); slotGeneration-window-preservation assertion added.
- `frontend/src/utils/transportBoundaries.regressions.test.ts`: decode-wipe test fixture moved to a next-day instant (`2026-01-03T00:15:00Z` = outside the picked `2026-01-02` UTC full civil day).
- Full-day fixture counts were generated with a throwaway node script using the project's `temporal-polyfill` (results embedded in the tests); script deleted.

## Important Decisions

- **Cross-midnight fixture is now unseedable (plan clarification, supersedes the 18-39-33 handoff's test-3 "4→2" framing):** under the new server full-day derivation, a POST seeding `2026-01-06T00:00:00Z`/`00:30:00Z` actives for a picked `2026-01-05` UTC event is outside the full civil day → 400; `seedCanonicalTimedEvent` asserts `response.ok()`. The wipe is enforced at the boundary, so reprojection test 3 must seed the 2 legal Jan-5 actives and may additionally assert the 4-actives payload gets 400. Alternative (server accepting window-wrapped next-day instants) rejected — contradicts plan item R3 ("boundary fixtures must move").
- When both the saved window and full-day domain are 00:00→00:00, the prior-day filter must NOT use `getLocalSlotDomainDay` with the event window (00:00 instants would map to the previous day and be wrongly dropped); plain local-date projection of in-domain actives is the correct membership-day mapping.
- `event.hasSpecificTimes` is not persisted for canonical timed events (server rejects it as a legacy field); the client value comes solely from the (fixed) `processEvent` heuristic.

## Findings And Risks

- Full unit suite: **7 failures remain** (4 test files), all locking the pre-contract read-only "full enabled day" rendering:
  - `useCalendarGrid.test.ts` — "uses activeSlots for normal-view specific-times cell occupancy..." (splitTimes rows now only actives-envelope, e.g. [570, 585] instead of [540..585]); "retains enabled specific-time rows outside a sparse active subset in GMT+5:30" (96 rows → 2).
  - `ScheduleOverlap.collapsedHours.test.ts` — "collapses enabled but inactive interior specific-time hours" and "keeps inactive specific-time hours collapsed while scheduling" (expected collapsed band "10 AM → 4 PM" rows now absent because the band collapses to actives).
  - `ScheduleOverlap.inactiveGapInteraction.test.ts` — "marks the collapsed hours inactive and clears the highlight on hover" and "keeps the enabled_inactive cell state when hovering an enabled but inactive gap".
  - `ScheduleOverlap.specificTimes.test.ts` — "renders the saved specific-times window immediately after saving a new event selection" (band immediately after save).
- Not yet run: lint, typecheck, build, Go suite, any e2e. Server, e2e specs, and diag-spec cleanup are untouched so far.
- Worktree: docs + all frontend source/test edits are unstaged on top of the prior staged state; diag specs still staged as new files (cleanup must use `git rm`-equivalent, not just `rm`); `frontend/backlog/inbox.md` remains unstaged user-modified (untouched).

## Validation

- `npx vitest run src/utils/timedEventSlots.test.ts src/composables/event/specificTimesEditDraft.test.ts src/utils/transportBoundaries.regressions.test.ts` (from `frontend/`): **85 passed**.
- `npx vitest run` (full unit suite): **7 failed | 860 passed** (136 files, 4 failing) — failures listed in Findings.
- Full-day fixture generation script (node + `temporal-polyfill`, run from `frontend/`) confirmed domain sizes: NY 96, weekly LA 48/day, sat-anchor/sun-filter 96/day, NY spring gap 92, NY fall overlap 100, Lord Howe spring 94/fall 98, UTC 48/96.

## Remaining Work

1. **Fix the 7 failing schedule-overlap unit tests** to the settled band contract (see Findings; the test bodies encode the OLD "read-only shows the complete enabled domain" behavior and must assert the actives-band instead).
2. **Server** — `deriveEnabledSlots` (`server/routes/event_timed_slots.go:93-140`): keep `parseLocalTime` validation, generate 00:00→00:00 next-day civil domain per generation day regardless of the window. Update `server/routes/event_timed_slots_test.go` (count/bounds mirror the new frontend fixtures) and `events_timed_payloads_test.go` (move boundary-rejection fixtures to cross-civil-day/other-day instants; add a range-create-accepted case where actives lie outside the stored window but inside the full day).
3. **E2E** (`frontend/e2e/`):
   - Reprojection spec test 3 (L204): rewrite — seed with only the 2 legal Jan-5 actives; expect 400 on the 4-actives payload; after no-op save assert actives = 2, 1 grid column, `slotGeneration 23:00→01:00` + `selectedDays ["2026-01-05"]` preserved.
   - Specific-times spec: delete test 5 (L169-226, tooltip anchor — abandoned design); update test 7 (L270) to full-day actives; move test 8's (L300) stale fixtures to other-day instants; verify tests 6/9 green.
   - Rerun utc4/weekly/create and reprojection 1-2 later; re-capture counts empirically after the band changes.
4. **Cleanup** — delete and unstage `frontend/e2e/timed-event-diag-probe-firefox.spec.ts` and `frontend/e2e/timed-event-diag-cross-midnight-firefox.spec.ts` (currently staged as new files).
5. **Gates** — frontend `npm run lint && npm run typecheck && npm run build && npm run test:unit`; Go via the isolated test overlay (`docker compose --env-file .env.development -f compose.yaml -f compose.test.yaml up -d mongo-test`, then `run --rm server-test`); Firefox e2e full with `PLAYWRIGHT_USE_EXISTING_SERVER=1` then chromium; verify dev stack (vite `127.0.0.1:4173`, `timeful-development-server-1` on 3002) with a quick curl before e2e.
6. **Stop** — report final state; no commit, no stack cleanup (`timeful-server-1` stays stopped, test stack as-is).

## Handoff Relationships

- Continues `handoff/handoff-2026-08-09T18-39-33Z.md` Remaining Work items 1-3, 6-8 (docs + frontend + server + e2e + gates); items 4 (band) and 5 (read-only band from actives) are code-complete but not yet verified by the failing schedule-overlap tests.
- Supersedes the 18-39-33 handoff's reprojection test-3 framing ("assert actives → 2 after no-op save" via a 4-actives seed): that fixture is now rejected at ingest (see Decisions).

## Suggested Skills

- `session-handoff` — this file was produced with it.

## Relevant Files

Changed this session (unstaged):
- `frontend/adr/012-frontend-timed-event-instant-slot-model.md`, `frontend/glossary.md`, `frontend/requirements/functional.md` — contract docs amended.
- `frontend/src/utils/timedEventSlots.ts` — full-day derivation, `getEventWindowRangeSlots`, actives-based coverage.
- `frontend/src/utils/general_utils.ts` — `processEvent` heuristic via window-range.
- `frontend/src/composables/event/specificTimesEditDraft.ts` — prior-day filter, window-preserving `slotGeneration`, disable restores full day.
- `frontend/src/composables/schedule_overlap/useCalendarGrid.ts` — band/axis/rows from actives (`savedSpecificTimesWindow`, `splitTimes`, `timedGridSlotProjection`).
- `frontend/src/utils/timedEventSlots.test.ts`, `frontend/src/composables/event/specificTimesEditDraft.test.ts`, `frontend/src/utils/transportBoundaries.regressions.test.ts` — rewritten to the new contract.

Not yet touched (plan anchors):
- `server/routes/event_timed_slots.go` (+ `event_timed_slots_test.go`, `events_timed_payloads_test.go`).
- `frontend/e2e/timed-event-reprojection-firefox.spec.ts` (test 3 at L204), `frontend/e2e/timed-event-specific-times-edit-firefox.spec.ts` (tests 5/7/8), diag specs (cleanup).

Environment: vite dev server `127.0.0.1:4173` up (existing PID, log `/tmp/opencode/vite-dev.log`); `timeful-development-server-1` on 3002 healthy + dev mongo up; `timeful-test-mongo-test-1` up (test stack, leave as-is); `timeful-server-1` stopped (leave as-is).