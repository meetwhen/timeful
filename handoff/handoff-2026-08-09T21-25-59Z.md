# Session Handoff

- Created session: `ses_017c5cf23ffemqea37qU2V1h1e`

## Completed Work

Carried out `handoff/handoff-2026-08-09T20-27-23Z.md` Remaining Work items 1-2: Go overlay re-verification done; first full Firefox e2e run (6 passed / 5 failed / 11 did not run) triaged; 3 root causes fixed with regression tests; gates green. A third full-suite run was started after the fixes and **aborted by the user** (results unknown). Chromium never run.

**1. Go overlay verified to completion.**
- `docker compose --env-file .env.development -f compose.yaml -f compose.test.yaml run --rm server-test go test ./routes -count=1` → `ok timeful/server/routes`.
- Verbose scoped run (`-run 'TestCreateEvent|TestDeriveEnabledSlots|TestNormalizeTimedEventPayloadFields'`) → **23 PASS** incl. Mongo-backed `TestCreateEventAcceptsActiveSlotsInsideFullDayOutsideWindow`.

**2. E2E firefox run 1 failures — 3 root causes, all fixed:**
- **Root cause A (systematic): fromEditEvent handoff gate.** `useScheduleOverlapController`'s `getInitialState` + the `fromEditEvent`/`fromCreateSpecificTimesDraft` watchers gated on `isSpecificTimes` (`event.hasSpecificTimes`). After the session's `general_utils.ts` change (`hasSpecificTimes` compares actives to the *window range*, not the full domain), range events (actives == window) decoded as not-specific-times, so an explicit "toggle specific times ON → Next" edit never entered the scheduling grid. Fixed in `useScheduleOverlapController.ts`: `fromEditEvent` (only ever set true by a toggle-ON submit) and `fromCreateSpecificTimesDraft` now trigger the handoff regardless of `isSpecificTimes`; `tempTimes` is still seeded from `event.activeSlots`. Three new regression tests in `useScheduleOverlapController.test.ts` written first (RED: 3 failed), fix applied (GREEN: 12/12).
- **Root cause B: stale dev server container.** `timeful-development-server-1` was built before the full-civil-day server derive; PUTs validated actives against the old window-scoped domain (URL evidence: `400 PUT … {"error":"active-slots-must-be-enabled"}` with a legal full-day payload). Rebuilt: `docker compose --env-file .env.development -f compose.yaml -f compose.development.yaml up -d --build server` → recreated, `GET /api/health` 200.
- **Root cause C: invalid seed fixtures under the full-day membership-day rule.** Reprojection "preserves timed instants when the event timezone changes…": actives LA `23:30`→Jan 5 `01:00` (07:30-09:00Z Jan 5) spilled past the Jan-4 LA civil-day end (08:00Z) → the new server rejects them at seed. Fixture rewritten: actives `2026-01-05T07:00:00Z`/`07:30:00Z` (LA Jan 4 23:00/23:30), window `23:00:00`→`01:00:00`; comment documents the constraint. Re-anchor to `["2026-01-05"]` still asserted.

**3. DST e2e column-count update.** Reprojection "reopens DST-boundary fixture…": the 23-hour LA spring-forward day spans UTC Mar 8 08:00Z → Mar 9 07:00Z, so the UTC viewer renders 2 columns (FR-026 projection). Added optional `expectedColumns` to `expectTimedFixtureReopen` (defaults to `expectedSelectedDays.length`); DST test passes `expectedColumns: 2`. Existing uniqueness assertion retained.

**4. Repo-tracked reruns after fixes:** `timed-event-reprojection-firefox.spec.ts` **4/4 passed**; `timed-event-specific-times-edit` "enabling specific-times and saving without grid edits" (L169) passed solo; `timed-event-timezone-menu` width test passed solo (it had failed only in the parallel full run — likely font-loading flake; unverified in a full run). Mobile-tooltip test (`.tw-fixed.tw-z-50` count after reload) — see Findings.

**5. Frontend gates green after the controller change.** `npm run lint` (0 errors, 3 pre-existing warnings), `npm run typecheck`, `npm run build` (4.39s), `npm run test:unit` → **136 files / 870 passed** (867 prior + 3 new controller tests).

**6. Cleanup.** Temporary probe spec `e2e/timed-event-probe-firefox.spec.ts` deleted; draft-probe `console.debug` logs removed from `specificTimesEditDraft.ts` via `git checkout --` (index/staged session work preserved, verified `git diff HEAD` still shows the staged 117-line rework).

## Important Decisions

- **User intent drives the grid handoff, not post-save data.** Toggling "Set specific times per day" in the editor and submitting (or handing off from the create dialog) always opens the scheduling grid with `tempTimes` seeded from saved actives, even when the event decodes as a range event (actives == window). Locked by 3 regression tests; consistent with the e2e contract (utc4 72-cell grid, weekly, reprojection test 1-2 flows).
- **Legal fixture rule under the full-day contract:** actives must lie inside membership days in the event timezone; post-midnight wrapped-window actives (next-day 00:00+) are rejected at ingest — matches the Go fixtures and e2e test 4's 400 assertion.
- **Dev stack rebuilt** to the current Go derive (in-scope per root AGENTS.md local workflow); `timeful-server-1` (old stack) and `timeful-test-mongo-test-1` left as-is.

## Findings And Risks

- The dissolved 5 failures were: fromEditEvent gate (utc4 weekly, reprojection 1-2 flows, specific-times edits), stale server (tz-change PUT 400), stale DST/spec fixtures (seed 400, column count), plus a parallel-run-only timezone-menu width flake.
- Post-fix full run (before the aborted rerun): 15 passed, 2 failed (reprojection tz-change then-failing 400; specific-times L169) — both verified green after their fixes. The mobile-tooltip test passed in that post-fix run; in the PRE-fix run it had failed at `.tw-fixed.tw-z-50` toHaveCount(0) → received 1 (its standalone failure mode before the create-gate fix). Classify with the next full run.
- The aborted rerun leaves **no full-suite green result**; timezone-menu (parallel flake) and the tooltip test are the two watch items.
- `frontend/backlog/inbox.md` still user-modified, untouched. No commit made; the user staged this session's fixes mid-session (`git status` shows them as staged: controller + tests, reprojection spec, etc.).

## Validation

- Go (both, via the isolated overlay + test Mongo `timeful-test-mongo-test-1`): `go test ./routes -count=1` → `ok`; verbose timed scope → 23 PASS.
- `npx vitest run src/components/schedule_overlap/useScheduleOverlapController.test.ts` → RED (3 failed) before fix, GREEN (12 passed) after.
- `npm run lint` → 0 errors (3 warnings); `npm run typecheck` → clean; `npm run build` → built 4.39s; `npm run test:unit` → 136 files / 870 passed.
- Post-fix file-level runs (from `frontend/`, `PLAYWRIGHT_USE_EXISTING_SERVER=1`): `timed-event-reprojection --project=firefox-desktop` → 4/4; `timed-event-specific-times-edit -g "enabling specific-times and saving without grid edits"` → 1 passed; `timed-event-timezone-menu --project=firefox-desktop` → 1 passed.
- POST-fix full firefox suite (before the final aborted rerun): 15 passed / 2 failed / 5 did not run (both failures subsequently fixed and re-verified).
- `timeful-development-server-1` recreated via compose.development.yaml build → `GET http://localhost:3002/api/health` 200 after 1 retry; vite `127.0.0.1:4173` still 200.

## Remaining Work

1. **Firefox full suite** (the rerun was aborted): from `frontend/`: `PLAYWRIGHT_USE_EXISTING_SERVER=1 npx playwright test --project=firefox-desktop` (firefox-desktop runs `workers: 1`; serial files abort remaining tests on failure — a single failure cascades). Watch items: mobile-tooltip test (`.tw-fixed.tw-z-50` toHaveCount(0) after reload) and `timed-event-timezone-menu` width (parallel flake passes solo).
2. **Chromium** (never run): `PLAYWRIGHT_USE_EXISTING_SERVER=1 npx playwright test --project=chromium-desktop`.
3. **Stop** — report final state; no commit; no stack teardown; `frontend/backlog/inbox.md` untouched. Note the dev server was recreated with the current Go build this session.

## Handoff Relationships

- Continues `handoff/handoff-2026-08-09T20-27-23Z.md` Remaining Work items 1-3: item 1 (Go overlay) done, item 2 (e2e) code/fix work done with file-level verification, full-suite + chromium still open, item 3 (final report) pending.
- Governing docs: `frontend/adr/012-frontend-timed-event-instant-slot-model.md` (read-only band from actives; full axis only in editor / `Show all hours`; derived full-day domain), `frontend/requirements/functional.md` FR-021/FR-026 (projection columns), `frontend/adr/0002-temporal-and-timezone-semantics.md`.

## Suggested Skills

- `session-handoff` — this file was produced with it.

## Relevant Files

Changed this session (staged by the user; check `git status`):
- `frontend/src/components/schedule_overlap/useScheduleOverlapController.ts` — fromEditEvent/fromCreateSpecificTimesDraft handoffs no longer gated on `isSpecificTimes`; `getInitialState` honors the explicit edit/create flags.
- `frontend/src/components/schedule_overlap/useScheduleOverlapController.test.ts` — 3 new regression tests (range-event edit redirect, mounted edit redirect, range-event create draft).
- `frontend/e2e/timed-event-reprojection-firefox.spec.ts` — test "preserves timed instants when the event timezone changes…" fixture rewritten (`2026-01-05T07:00:00Z`/`07:30:00Z`, window `23:00:00`→`01:00:00`); `expectTimedFixtureReopen` gains `expectedColumns`; DST test passes `expectedColumns: 2`.

Inspect-only (diagnostics deleted/reverted; no residue):
- `frontend/e2e/timed-event-probe-firefox.spec.ts` (deleted), `frontend/src/composables/event/specificTimesEditDraft.ts` (probe logs reverted to staged state).

Unchanged but relevant context: `frontend/src/utils/general_utils.ts` (`hasSpecificTimes` vs window range — the semantic that surfaced the gate regression), `server/routes/event_timed_slots.go` (full civil-day derive — the reason the dev server needed rebuilding).

Environment: vite dev server `127.0.0.1:4173` up (log `/tmp/opencode/vite-dev.log`); `timeful-development-server-1` on 3002 **recreated with the current Go build** (healthy) + dev mongo; `timeful-test-mongo-test-1` up (test stack, leave as-is); `timeful-server-1` stopped (leave as-is).