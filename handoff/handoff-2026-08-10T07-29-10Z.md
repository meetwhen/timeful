# Session Handoff

- Created session: `ses_0158f0771ffeqNScfn07m4PYfl`

## Completed Work

Continued `handoff/handoff-2026-08-09T21-25-59Z.md` Remaining Work items 1-2 (Firefox full suite, first-ever Chromium run) and item 3 (final report, delivered as this handoff). Environment was already up: vite `127.0.0.1:4173` 200, dev server `localhost:3002/api/health` 200. Prior session's controller/spec fixes were already committed (`ec5fd3e8 chore: commit`).

**Per-file Firefox timing detection** (the "suite timed out" concern). Ran each firefox spec file scoped with `time` (parallel batch, all passed except noted); no file hangs:
- `timed-event-reprojection` 4/4 ≈ 1.2m; `timed-event-create` 6/6 ≈ 1.8m; `weekly+utc4+timezone-menu` 4/4 ≈ 1.2m; `schedule-overlap-mobile-touch` (firefox-touch) ≈ 1.2m; `timed-event-specific-times-edit` ≈ 1.6m (1 fail = L225, 2 DNR).
- Full firefox-desktop suite completes in ~2.5-4m; the "timeout" was per-test 30s timeouts on flaky tests, not a hang.

**Firefox full suite (firefox-desktop, 22 tests, serial files cascade on failure):**
- Run 1: 18 passed / 2 failed / 2 DNR (3.9m). Failures were 30s **test timeouts** (not assertions): L228 "disabling specific-times…" (`page.waitForResponse` PUT) and utc4 L13 (`waitForSpecificTimesGrid`). Both passed solo → load-sensitive timeouts, not regressions.
- Run 2: 14 passed / 1 failed / 7 DNR (2.5m). Failure = L25 "mobile grid does not show a tooltip after reload…" `.tw-fixed.tw-z-50` toHaveCount(0) → received 1 (the handoff's documented watch item).

**Stale-test fixture fix (L225).** Deterministic failure: local `buildFullDayUtcRange` (`endHour: 23, endMinute: 59, incrementMinutes: 60`) built an exclusive end at next-day 00:59, so it included `2026-05-30T00:00:00Z` plus a duplicated `2026-05-29T00:00:00Z` (`sortIsoInstants` sorts, does not dedupe). Under the full-civil-day/membership-day rule the sibling test L258 asserts that exact `2026-05-30T00:00:00Z` is a stale slot rejected with 400, so the frontend/server correctly omit it. Fixed the helper to `endMinute: 0` (exclusive end = next-day 00:00, i.e. 00:00-23:00, 24 slots/day, no spill) and updated the comment. Verified solo: 1 passed (13.7s). This is the only repo change this session, left **uncommitted** per the no-commit instruction.

**Chromium run (`--project=chromium-desktop`, never run before):** 13 tests → 8 failed / 2 passed / 3 skipped (24.2s). Two distinct buckets:
- **Config mismatch (4 fails):** all 4 `schedule-overlap-mobile-touch-firefox.spec.ts` tests. 3 failed `touchscreen.tap: hasTouch must be enabled on the browser context` (chromium-desktop has no touch); spec:4 failed waiting on the `.schedule-overlap-mobile-overlay` "Responses" heading (1440px viewport → `isPhone` false → no mobile overlay, so `hasTouch` alone is not sufficient). The chromium projects use `testIgnore: /timed-event-.*firefox\.spec\.ts/`, which only ignores `timed-event-*` filenames; this spec leaks into both chromium projects despite being authored for `firefox-touch`.
- **Non-mobile desktop failures (separate triage, 4 fails):** landing-hero:39 (desktop contract) — `.landing-hero-subtitle` not in DOM because `Landing.vue:69` renders it under `v-if="landingSignInEnabled"` (flag off in this runtime); `event-description-header-layout:11` — gap math expected ≤4, received 32; `event-page-no-responses-layout:11` — strict-mode violation, `.schedule-overlap-time-grid__body-row` resolved to 2 elements; `sign-up-form-event:6` — create POST `response.ok()` false.
- 3 skipped (correct self-guards): landing-hero:76 (requires `isMobile`), `schedule-overlap-mobile-scroll` (requires chromium-mobile), `schedule-overlap-pager-button-sizes` (requires chromium-mobile). 2 passed: both `event-page-days-only-layout` tests.

**Mobile/touch emulation answer.** Playwright runs desktop binaries even for "mobile" projects; mobile is context flags. `chromium-mobile` = `devices["iPhone 13"]` (`hasTouch: true, isMobile: true`, 390×664); `firefox-touch` = firefox + `hasTouch: true`, 375×900. The touch spec is not "unfixable" — it needs `hasTouch` **and** a narrow viewport (mobile UI is `isPhone`/sm-breakpoint gated); it already has correct homes (firefox-touch, and chromium-mobile would work too). The synthetic-event mobile tests in `timed-event-specific-times-edit` (L25-L167) prove the pattern: `setViewportSize(375,900)` + dispatched synthetic events, no touch API needed. No config/product changes were made this session.

**Mobile-tooltip flakes (pre-existing, classified, not product-fixed).** L25 fails 2/3 solo (received 1). Cause: Firefox re-fires `mouseover` for the persisted cursor position after `page.reload`; on phone the hover-tooltip is **intentional** — `useTimedGridInteractions.ts` `mouseover` handler sets `selectedTooltipSlot` (src/components/schedule_overlap/useTimedGridInteractions.ts:202), covered by unit test `ScheduleOverlap.mobileTooltip.test.ts:22`, and `mouseleave` keeps it on phone (:223). Not a state-persistence bug; not this session's code (file untouched). Firefox-touch specs also flake in the shared create helper (`ensureSpecificTimesEditorMode` → `isSpecificTimesEnabled` at helpers:201) under load: L62 and L149 each failed in different solo file runs.

Watch items were the two 30s-timeout flakes (full-run-only), the L25 hover/reload flake (handoff watch item, now confirmed at 2/3 solo), and the firefox-touch helper flake.

## Important Decisions

- **L225 expectation aligned to the membership-day rule, not to the frontend.** The stored 48-slot set (day-28 00:00 → day-29 23:00) is the correct canonical full-day domain; the stale fixture in the test was fixed, product code untouched.
- **L25 tooltip behavior is intentional and not a regression.** Hover-shows-tooltip on phone is unit-locked; the e2e's lingering-cursor-after-reload scenario is the source of nondeterminism. Left un-fixed to avoid changing deliberate behavior; documented for a follow-up (make the e2e robust instead).
- **Chromium-desktop should not run touch/mobile-emulation specs.** Only `schedule-overlap-mobile-touch-firefox` currently leaks; mobile specs belong to `chromium-mobile` / `firefox-touch`. Recommended fix is config (testIgnore) + optional `hasTouch` self-guard, mirroring sibling specs — implementation deferred (see Remaining Work).
- **No commit, no stack teardown, `frontend/backlog/inbox.md` untouched.** The L225 fixture fix stays as an unstaged worktree change.

## Findings And Risks

- Whole-suite firefox runs do **not** hang (~2.5-4m); earlier "timed out" reports were per-test 30s timeouts and/or the aborted rerun. Watch flaky tests individually, not the wall clock.
- Full-suite flakes move between runs (run 1: L228 + utc4 timeouts; run 2: L25 tooltip). All pass per-file/solo except L25 (deterministic-ish at ~2/3 solo) and the firefox-touch helper flake.
- Chromium suite was first-ever run and surfaced 4 non-mobile failures that are likely never-run-before spec-vs-runtime mismatches (feature-flag-gated hero subtitle, metric/locator drift, failed create POST), plus the mobile-touch config leak.
- No single firefox spec is a time hog; if runtime vs wall-clock still feels slow, suspect the 30s stalled tests rather than a hang.

## Validation

- Firefox per-file timing batch (all from `frontend/`, `PLAYWRIGHT_USE_EXISTING_SERVER=1`, `--reporter=line`) with per-file wall times: reprojection 4/4 (1:16); specific-times 5/1/2 (1:36); create 6/6 (1:49); weekly+utc4+timezone-menu 4/4 (1:13); mobile-touch (firefox-touch) 3/1 (1:18).
- L225 after fix, isolated: 1 passed (13.7s).
- L149 isolated: 1 passed (20.6s); full `schedule-overlap-mobile-touch` file solo: 3 passed / 1 failed (L62, create-helper flake).
- L25 isolated ×3: pass, fail, fail (received 1 each time).
- Full firefox-desktop run 1 → 18/2/2 (3.9m); run 2 → 14/1/7 (2.5m). Logs: `/tmp/opencode/firefox-full-run.log`, `/tmp/opencode/firefox-full-run2.log`.
- Chromium-desktop → 8/2/3 (24.2s). Log: `/tmp/opencode/chromium-desktop.log`.
- Environment: vite `127.0.0.1:4173` 200, `localhost:3002/api/health` 200 at session start; servers left running.

## Remaining Work

1. **Chromium project-config fix (recommended Option A):** add `/schedule-overlap-mobile-touch-firefox\.spec\.ts/` to the chromium projects' `testIgnore` in `frontend/playwright.config.ts` (it already has a dedicated `firefox-touch` project). Optional hardening: add a `hasTouch` self-guard to that spec (same pattern as `schedule-overlap-mobile-scroll`/`schedule-overlap-pager-button-sizes`). Then run `--project=chromium-desktop` (expect the 4 touch failures gone, only the 4 non-mobile ones remain) and `--project=chromium-mobile` (expect `mobile-scroll` + `pager-button-sizes` to pass).
2. **Decision point:** run `schedule-overlap-mobile-touch` under `chromium-mobile` too (cross-browser touch coverage) or keep it firefox-only per its filename? Default = firefox-only.
3. **Triage the 4 non-mobile chromium failures** (separate from the touch question): landing-hero:39 (`landingSignInEnabled` flag gate), `event-description-header-layout:11` (gap ≈32 vs ≤4), `event-page-no-responses-layout:11` (strict-mode `2 elements` locator), `sign-up-form-event:6` (create POST non-ok — inspect the API body/status).
4. **L25 e2e robustness** (optional): make the firefox tooltip test non-racy without changing the intentional hover-tooltip behavior (e.g., avoid leaving the cursor over a slot across reload) or accept/annotate the documented flake; assess firefox-touch create-helper flake (helpers:201) separately.
5. **Stop:** no commit planned; do not tear down the stack; `frontend/backlog/inbox.md` untouched. Note: `frontend/e2e/timed-event-specific-times-edit-firefox.spec.ts` (L228 test fixture fix) is an unstaged worktree change waiting on the user's commit decision.

## Handoff Relationships

- Continues `handoff/handoff-2026-08-09T21-25-59Z.md` Remaining Work 1-2: firefox full suite now run (flakes documented, 1 stale fixture fixed), chromium first run done (failures bucketed); the old item 3 (final report) is superseded by this handoff's Remaining Work.
- Governing docs: `frontend/adr/012-frontend-timed-event-instant-slot-model.md` (full-civil-day domain via actives; membership-day rule, no next-day 00:00 spill), `frontend/requirements/functional.md` FR-026 (projection columns), `frontend/playwright.config.ts` (project/testMatch/testIgnore matrix).

## Suggested Skills

- `session-handoff` — this file was produced with it.

## Relevant Files

Changed this session (unstaged; user commit decision pending):
- `frontend/e2e/timed-event-specific-times-edit-firefox.spec.ts` — local `buildFullDayUtcRange` fixture fix (`endMinute: 59` → `0`) for the "disabling specific-times restores active slots to the full enabled domain" test (now at ~L228); comment documents the membership-day boundary.

Unchanged this session but central to the findings:
- `frontend/playwright.config.ts` — project matrix; chromium `testIgnore` gap that leaks the touch spec into chromium-desktop.
- `src/components/schedule_overlap/useTimedGridInteractions.ts` — phone hover-tooltip (`mouseover` at :202) that L25's lingering-cursor reload scenario re-triggers.
- `src/components/schedule_overlap/ScheduleOverlap.mobileTooltip.test.ts:22` — unit-locks the hover-tooltip behavior.
- `src/views/Landing.vue:69` — `landingSignInEnabled` v-if that gates `.landing-hero-subtitle` (chromium landing-hero:39 failure).
- `frontend/e2e/helpers/timed-event-helpers.ts` — `ensureSpecificTimesEditorMode`/`isSpecificTimesEnabled` (:201) create-helper flake point; `sortIsoInstants` (sort-only, no dedupe).

Inspected specs (never-run-before status): `e2e/schedule-overlap-mobile-scroll.spec.ts`, `e2e/schedule-overlap-pager-button-sizes.spec.ts`, `e2e/landing-hero.spec.ts`, `e2e/event-description-header-layout.spec.ts`, `e2e/event-page-no-responses-layout.spec.ts`, `e2e/sign-up-form-event.spec.ts`, `e2e/timed-event-utc4-edit-firefox.spec.ts`.

Environment: vite `127.0.0.1:4173` up (log `/tmp/opencode/vite-dev.log`); `timeful-development-server-1` on 3002 healthy (rebuilt with the current Go build in the prior session) + dev mongo; `timeful-test-mongo-test-1` up (test stack, leave as-is); `timeful-server-1` stopped (leave as-is). Full-run/Chromium logs in `/tmp/opencode/`.
