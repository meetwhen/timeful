# Session Handoff

- Created session: `ses_01551443effeARsC5e3vQ0rE5C`

## Completed Work

Worked the `handoff/handoff-2026-08-10T07-29-10Z.md` Remaining Work items 1-2 (Chromium project-config fix + cross-browser touch decision). Environment was already up: vite `127.0.0.1:4173` 200, dev server `localhost:3002/api/health` 200, `timeful-development-server-1` + dev mongo + `timeful-test-mongo-test-1` healthy.

**Item 1 — Chromium `testIgnore` fix (recommended Option A), done.** In `frontend/playwright.config.ts`, changed both chromium projects' `testIgnore` from a single regex to an array adding `/schedule-overlap-mobile-touch-firefox\.spec\.ts/` (the spec already has its own `firefox-touch` project). Verified the 4 touch-suite failures no longer run under chromium.

**Item 2 — Decision + optional hardening, done (default = firefox-only).** Kept the touch spec firefox-only (excluded from both chromium projects). Also added the optional `hasTouch` self-guard to `frontend/e2e/schedule-overlap-mobile-touch-firefox.spec.ts` via a top-level `test.beforeEach(({ hasTouch }) => test.skip(!hasTouch, ...))`, mirroring the self-guard pattern of `schedule-overlap-mobile-scroll`/`schedule-overlap-pager-button-sizes`. lint flagged two iterations (empty object pattern, then a boolean literal compare) before settling on the fixture-destructured `!hasTouch` form.

**Item 3 — Partial triage, one new failure explained.** The prior handoff expected `schedule-overlap-mobile-scroll` and `schedule-overlap-pager-button-sizes` to pass under `chromium-mobile`. `pager-button-sizes` now passes; `mobile-scroll` does **not** (see Findings). This is a never-run-before spec (chromium-mobile's first full run) and now blocks clean triage of the chromium-mobile baseline.

## Important Decisions

- **Config fix, not a product change.** Touch-suite exclusion restricts mobile-emulation specs to their authored homes (`firefox-touch` / `chromium-mobile`); product code untouched this session.
- **`hasTouch` self-guard is firefox-only-compatible.** Chrome-emulated `chromium-mobile` also sets `hasTouch` (iPhone 13), but the spec is excluded there by `testIgnore`, so the guard is defense-in-depth only. `firefox-touch` still runs all 4 touch tests.
- **No commit, no stack teardown.** Both files remain unstaged; `frontend/e2e/timed-event-specific-times-edit-firefox.spec.ts` (prior session's L225 fixture fix) and `frontend/backlog/inbox.md` (pre-existing modified) unchanged by me.

## Findings And Risks

- **Chromium-desktop post-fix: 4 failed / 3 skipped / 2 passed (~12-17s).** The 4 failures are exactly the handoff's expected non-mobile set: `event-description-header-layout:11`, `event-page-no-responses-layout:11`, `landing-hero:39`, `sign-up-form-event:6`. The 4 touch failures are gone (verified repeatedly). 2 passed = both `event-page-days-only-layout` tests.
- **Chromium-mobile: 5 failed / 3 skipped / 1 passed (33.2s).** Failures: `event-description-header-layout:11`, `event-page-no-responses-layout:11`, `landing-hero:76` (screenshot compare), `schedule-overlap-mobile-scroll:8`, `sign-up-form-event:6`. `pager-button-sizes` passes. 1 passed = one `event-page-days-only-layout`; `landing-hero:39` skips (desktop contract) on mobile.
- **`schedule-overlap-mobile-scroll:8` fails at `#drag-section .timeslot[data-row="1"][data-col="0"]` ("Element is not attached to the DOM")** after `createSpecificTimesEventFromDialog` + `dragSelectGridRange` + `saveEditorAndWaitForPut({ action: "next" })`. Debug (`createSpecificTimesEventFromDialog` step-through via temp specs `debug2`-`debug5`, since deleted) shows the read-only grid after that flow renders a **degenerate single row**: only `data-row="0"` (2 cell columns = 2 day columns), timeslot height 15px = 15-min increments (`HOUR_HEIGHT/4`), 1 body row, 0 collapsed rows. The fetched event has `slotGeneration: startTimeLocal 00:00, endTimeLocal 00:00, timeIncrementMinutes 15`, `eventTimezone Europe/Moscow`. In contrast, a `seedCanonicalTimedEvent`-seeded specific-times event (09:00-11:00 active, 60-min increment) renders a full multi-row grid (rows 0-2+ at 60px). So the grid *can* render full rows on read-only; the create-dialog path collapses to one row. Whether this is a stale spec (the read-only grid legitimately collapses to the active subset, `showAllHours=false` preference) or a product regression is unresolved; the spec's 15px-row finding contradicts its own `data-row="1"` assumption.
- **Alternative failure mode in the same flow: the known create-helper flake.** One run of `createSpecificTimesEventFromDialog` timed out at 30s inside `isSpecificTimesEnabled` (helpers:201 via `ensureSpecificTimesEditorMode`), matching the handoff-documented firefox-touch helper flake; it also affects this chromium spec. Retry-in-success.
- **`firefox-touch` touch spec still runs (self-guard pass-through): 3 passed / 1 failed** in the post-guard verification run; the 1 failure is the previously documented tooltip-navbar layering flake (arg at L156/L224), not a self-guard regression.
- **Landing-hero:76 (mobile)** now fails on a `toHaveScreenshot` compare — a screenshot baseline mismatch surfaced only on first `chromium-mobile` run; needs a snapshot review, not just locator review.

## Validation

- Config + guard edits: `cd frontend && npm run lint` → 0 errors (3 pre-existing `vue/require-default-prop` warnings in `RespondentsList.vue`); `npx tsc --noEmit -p tsconfig.json` → exit 0.
- `PLAYWRIGHT_USE_EXISTING_SERVER=1 npx playwright test --project=chromium-desktop --reporter=dot` → 4 failed / 3 skipped / 2 passed (17.5s). Log: `/tmp/opencode/chromium-desktop-postfix.log` (terminal output).
- Same for `--project=chromium-mobile` → 5 failed / 3 skipped / 1 passed (33.2s).
- `--project=firefox-touch e2e/schedule-overlap-mobile-touch-firefox.spec.ts` → 3 passed / 1 failed (40.3s), confirming non-skip under touch.
- `--project=chromium-desktop --project=chromium-mobile e2e/schedule-overlap-mobile-touch-firefox.spec.ts` → "No tests found" (testIgnore working as intended).
- Temp debugging specs `frontend/e2e/debug2-5.spec.ts` created and deleted; no residue.
- Environment left up as found; no stack teardown.

## Test Layering Update (appended after this session)

Test-layering follow-up executed in a later session:

- Relocated the raw-POST API rejection checks out of browser e2e: the server route test `TestCreateEventRejectsActiveSlotsOutsideDerivedDomain` (and its weekly sibling) already covered the HTTP 400 + `active-slots-must-be-enabled` mapping, so the browser duplicates were removed. Deleted the stale-active-slots e2e test and the raw-POST half of the reprojection cross-midnight test.
- Deleted the L25 "mobile grid does not show a tooltip after reload" e2e flake (behavior stays unit-locked in `ScheduleOverlap.mobileTooltip.test.ts` / `useTimedGridInteractions.test.ts`).
- Trimmed redundant grid-cell-class/payload assertions to smoke level in `timed-event-create-firefox.spec.ts` and `timed-event-reprojection-firefox.spec.ts`; coarsened `event-description-header-layout.spec.ts` pixel tolerances.
- Open items from here (mobile-scroll collapse question, landing-hero mobile screenshot baseline) tracked in `frontend/backlog/inbox.md`.

## Remaining Work

1. **Resolve `schedule-overlap-mobile-scroll:8` under `chromium-mobile`.** Decide whether the read-only grid, after the create-dialog flow, collapsing to a single `data-row="0"` 15px row is intended (then fix the spec — e.g. seed via `seedCanonicalTimedEvent` like `pager-button-sizes` instead of the create dialog, or assert against the collapsed row) or a product regression (then fix product + keep spec). Establish the `showAllHours`/collapsed-hours behavior for a freshly created 15-min specific-times event on read-only first.
2. **Triage the shared non-mobile failures** (both chromium projects): `landing-hero:39` (`.landing-hero-subtitle` gated behind `landingSignInEnabled` in `src/views/Landing.vue:69`), `event-description-header-layout:11` (gap math, received ~32 vs expected ≤4), `event-page-no-responses-layout:11` (strict-mode locator resolved to 2 elements), `sign-up-form-event:6` (create POST `response.ok()` false — inspect API status/body).
3. **Review `landing-hero:76` mobile screenshot baseline** for `chromium-mobile` (`toHaveScreenshot` failure; snapshot comparison, not locator).
4. **Update the handoff's watch-list flake claims:** re-confirm the touch-spec create-helper flake now also affects `chromium-mobile` runs of the shared helper.
5. **Stop rules:** no commit; do not tear down the stack; `frontend/backlog/inbox.md` untouched. Unstaged worktree changes: `frontend/playwright.config.ts`, `frontend/e2e/schedule-overlap-mobile-touch-firefox.spec.ts` (this session), `frontend/e2e/timed-event-specific-times-edit-firefox.spec.ts` + `frontend/backlog/inbox.md` (pre-existing).

## Handoff Relationships

- Continues `handoff/handoff-2026-08-10T07-29-10Z.md` Remaining Work items 1-2 (config fix + touch decision, done) and item 3 (triage of the 4 non-mobile failures — now extended by 2 chromium-mobile-only failures: `mobile-scroll` and the `landing-hero:76` screenshot). Prior item 1's "expect mobile-scroll + pager-button-sizes to pass" slightly overshot: pager passes, mobile-scroll does not.
- Governing docs: `frontend/playwright.config.ts` project/testIgnore matrix; `frontend/adr/012-frontend-timed-event-instant-slot-model.md` (full-civil-day domain, membership-day rule) if the collapsed read-only grid is investigated as product behavior.

## Suggested Skills

- `session-handoff` — this file was produced with it.

## Relevant Files

Changed this session (unstaged; no commit decision made yet):
- `frontend/playwright.config.ts` — chromium projects' `testIgnore` now also `/schedule-overlap-mobile-touch-firefox\.spec\.ts/`.
- `frontend/e2e/schedule-overlap-mobile-touch-firefox.spec.ts` — top-level `test.beforeEach(({ hasTouch }) => test.skip(!hasTouch, ...))` self-guard.

Carried over (pre-existing, unchanged):
- `frontend/e2e/timed-event-specific-times-edit-firefox.spec.ts` — prior session's L225 `buildFullDayUtcRange` fixture fix (unstaged).
- `frontend/backlog/inbox.md` — modified relative to HEAD, pre-existing.

Inspected for the mobile-scroll triage (unchanged): `frontend/src/components/schedule_overlap/scheduleOverlapGridRows.ts` (`buildRenderedTimeGridRows`, `buildPageSlots`, `buildCollapsedPageSegments`), `useTimedGridPresentation.ts` (`renderedRows`, `canCollapseTimes`), `ScheduleOverlapTimeGrid.vue` (`#drag-section`, 15px row height source), `useCalendarGrid.ts` (`timeslotHeight` = `HOUR_HEIGHT/4` for 15-min), `ScheduleOverlap.collapsedHours.test.ts:308` (read-only specific-times collapse-to-active-subset unit contract), `frontend/e2e/helpers/timed-event-helpers.ts` (`createSpecificTimesEventFromDialog`, `saveEditorAndWaitForPut`, `seedCanonicalTimedEvent`, `buildSpecificDateSeed`, `fetchEventByShortId`, `isSpecificTimesEnabled` at :201).

Environment: vite `127.0.0.1:4173` up; `timeful-development-server-1` 3002 healthy + dev mongo; `timeful-test-mongo-test-1` up (leave as-is); `timeful-mongo-1`/`timeful-server-1` left as-is.
