# Session Handoff

- Created session: `ses_00414422fffeT77Ci4V8BhL1vJ`

## Retained Context

- Timed events persist only `activeSlots`. Derive enabled, inactive, and disabled domains through `frontend/src/utils/timedEventSlots.ts` (`getEventEnabledSlots`); the server independently derives and validates the enabled domain in `server/routes/event_timed_slots.go`.
- Legacy timed events carry their full domain in internal `times`, populated from non-empty transport `enabledSlots` first and then legacy `times`; `activeSlots` remains the selected subset. Contract events derive their domain from recurrence, timezone, slot generation, and selected dates or weekdays.
- Canonical timed slots span each picked event-timezone civil day. Read-only grids project enabled slots and collapse inactive spans; editors show the complete enabled domain. Keep range versus specific-times classification based on active slots versus the stored window, not the enabled domain.
- For weekly events, derive the anchor from the earliest active slot. Empty active sets use the current-time fallback. Weekly edits pass `weeklyAnchorInstant` through `eventEditorSchedule.ts`; recurrence must not overwrite a group's `type` during transport decoding.
- When changing a specific-date event timezone, preserve active instants by re-anchoring membership dates to their new local dates when needed. Reject active instants outside the event's enabled civil-day domain.
- Consult `frontend/adr/012-frontend-timed-event-instant-slot-model.md`, `frontend/glossary.md`, and FR-018, FR-021, FR-025, FR-026, and FR-027 in `frontend/requirements/functional.md` for timed-grid semantics. Lord Howe fall-back windows at the fold edge remain a Go/Temporal parity caveat.
- `ScheduleOverlap.vue` is the composition root. Keep grid rows, timed-grid presentation/interactions, and guest actions in their focused modules. `TimeFormatToggle.vue` is intentionally local rather than shared with `SlideToggle`.
- Schedule-overlap tooltips use structured `TooltipSegment[]`; only time-range segments are monospace. Grid labels are driven by the selected time format and preserve DST offset suffixes. Twelve-hour labels use uppercase `AM` and `PM`.
- The Responses panel uses per-slot status squares for available, if-needed, unavailable, enabled-inactive, and out-of-range states; retain avatars when no slot is in context. Non-selectable grid cells clear selection, highlight, and tooltip.
- Range-event grids omit the light-grey `Disabled, change in Edit event` legend item. Aggregate heatmap opacity scales by simultaneous availability: `#00994CE1` for non-universal peaks and `#00994CFF` for universal peaks.
- Compact desktop schedule controls are intentionally left-aligned in two rows. Desktop timezone styling uses scoped Vuetify internals and should be visually rechecked after Vuetify upgrades.
- Chromium projects exclude `schedule-overlap-mobile-touch-firefox.spec.ts`; it runs only in `firefox-touch`. Playwright touch emulation is not physical Firefox-mobile verification. Before declaring mobile behavior verified, test Responses-panel tap isolation, outside dismissal, and visibility/`mouseleave` behavior on a real Firefox mobile device.
- Investigate and stabilize the shared `ensureSpecificTimesEditorMode` / `isSpecificTimesEnabled` E2E timeout in Firefox-touch and Chromium-mobile, plus the intermittent Firefox-touch tooltip/navbar layering failure. Re-run `schedule-overlap-mobile-scroll:8`; its single-row diagnosis predates the enabled-domain projection and collapsed-hours behavior.
- Investigate the non-OK event POST in `frontend/e2e/sign-up-form-event.spec.ts` by capturing its status and response body, then compare the payload with `server/routes/events.go` timed-payload normalization.
- Before production deployment, verify the status of `server/scripts/20260724_canonical_timed_events`; deploy the server before a frontend requiring derived enabled-slot validation.
- For unit-test performance investigations, use `npm run test:unit:profile`, retain Vitest `slowTestThreshold: 100` and default worker scheduling unless benchmarked in target CI, and remove `happy-dom` only from verified Node-safe suites.
