# Session Handoff

- Created session: `ses_ffe972f27ffe5SJrQp0LWSSSS8`

## Session Objective

Complete remaining-work item 6 from `handoff/handoff-2026-08-14T16-13-13Z.md`: add rollout verification gates and prepare a staging-only PostgreSQL rollout. Implementation and local validation were completed; no staging deployment was performed because this workspace has no staging-host access.

## Current State

- PostgreSQL coexistence implementation remains uncommitted on top of the existing five PostgreSQL commits.
- The isolated MongoDB/PostgreSQL test stack is retained because browser checks used `TEST_DB_PERSIST=true` to avoid deleting pre-existing local test volumes.
- `frontend/backlog/inbox.md` and `handoff/answers` were already user changes and were not modified by this session.

## Completed Work

- Replaced the live Microsoft Graph request test and embedded credential with a hermetic HTTP transport fixture in `server/services/microsoftgraph/microsoftgraph_test.go`.
- Restored `go test ./...` compatibility for three historical one-off migration programs under `server/scripts/`.
- Added dual-database backend CI in `.github/workflows/backend-ci.yml`; expanded frontend CI with lint and typecheck in `.github/workflows/frontend-ci.yml`.
- Made PostgreSQL test bootstrap idempotent and forced the test server `APP_ENV=test` in `compose.test.yaml`.
- Made Playwright test mode bind Vite to `127.0.0.1:4174` and proxy to the isolated server on `127.0.0.1:3003`, independent of local `.env.test` drift, in `frontend/config/tooling.ts`.
- Added an opt-in Firefox PostgreSQL anonymous-poll/plugin lifecycle spec in `frontend/e2e/timed-event-postgres-plugin-firefox.spec.ts`; its flag is wired through `frontend/e2e/isolated-test-stack.ts`.
- Added the staging operational procedure in `docs/postgres-staging-rollout.md` and corrected isolated route-test commands in `docs/environments.md`.
- Made the existing schedule-overlap UI storage read robust when Node exposes unusable `localStorage`, restoring the unit suite in `frontend/src/composables/schedule_overlap/useScheduleOverlapUI.ts`.

## Important Decisions

- Standard browser E2E remains Mongo-creation by default. PostgreSQL browser coverage is explicitly enabled with `E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true` and runs only the dedicated lifecycle spec.
- The staging boundary remains staging only, as selected by the user. The release procedure starts with PostgreSQL creation disabled and treats disabling the flag as the creation rollback.

## Findings And Risks

- The dedicated PostgreSQL Firefox test passes, covering namespaced long/short IDs and plugin `set-slots`/`get-slots` behavior.
- The full standard Firefox suite still has intermittent existing specific-times editor-grid timeouts. Its final run had 11 passed, 1 skipped, and 3 failed at `specific-times-grid-next` visibility; these failures do not occur in the dedicated PostgreSQL lifecycle spec.
- The formerly embedded Microsoft Graph credential was removed from the repository, but its external revocation/rotation still requires action by the credential owner.
- PostgreSQL backup and recovery automation remain deferred, as documented in the prior handoff and rollout document.

## Dependencies And Prerequisites

- Before staging rollout, revoke/rotate the prior Microsoft Graph credential outside the repository.
- Populate required PostgreSQL bootstrap, migrator, application, and backup-role variables in `.env.staging`; do not commit credentials.
- Staging deployment requires access to the staging host and its environment files.

## Validation

- Passed: `POSTGRES_TEST_DATABASE=timeful-test-postgres docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml config --quiet`.
- Passed: `POSTGRES_TEST_DATABASE=timeful-test-postgres docker compose --env-file .env.test -f compose.yaml -f compose.test.yaml run --rm server-route-test go test ./... -count=1`.
- Passed: `npm run lint`, `npm run typecheck`, `npm run test:unit` (892 tests), and `npm run build` from `frontend/`.
- Passed: `TEST_DB_PERSIST=true E2E_POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=true npm run test:e2e -- --project=firefox-desktop timed-event-postgres-plugin-firefox.spec.ts`.
- Did not pass: the full `TEST_DB_PERSIST=true npm run test:e2e -- --project=firefox-desktop` suite due to the existing editor-grid timeouts described above.
- Staging Compose validation, deployment, smoke testing, and flag enablement were not run.

## Remaining Work

1. Investigate and stabilize the standard Firefox specific-times editor-grid timeouts before treating the full browser suite as a release gate.
2. Revoke/rotate the Microsoft Graph credential removed from the test history.
3. Deploy staging according to `docs/postgres-staging-rollout.md`, initially with `POSTGRES_ANONYMOUS_EVENT_CREATION_ENABLED=false`, then execute its enabled-flag smoke test.

## Next Recommended Action

Run the three failing Firefox specific-times tests individually with retained test state and inspect their Playwright error contexts to identify why the editor does not render `specific-times-grid-next`.

## Handoff Relationships

- Continues `handoff/handoff-2026-08-14T16-13-13Z.md` remaining-work item 6.

## Suggested Skills

- `commit`: use only if the user asks to commit the rollout-gate changes.

## Relevant Files

- `.github/workflows/backend-ci.yml`: new full backend and PostgreSQL browser CI gate.
- `compose.test.yaml`: idempotent database bootstrap and isolated server environment.
- `frontend/config/tooling.ts`: deterministic Playwright test proxy configuration.
- `frontend/e2e/timed-event-postgres-plugin-firefox.spec.ts`: passing PostgreSQL lifecycle regression.
- `docs/postgres-staging-rollout.md`: staging rollout and rollback procedure.
- `handoff/handoff-2026-08-14T16-13-13Z.md`: compatibility-first PostgreSQL migration direction.
